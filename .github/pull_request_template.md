## Что меняется

<!-- Что было, почему это оказалось неверным, что стало. Если чинит issue — Fixes #N. -->

## Чем проверено

<!-- Тесты, замер, живой запуск. Если что-то не проверено — так и напишите. -->

## Перед отправкой

- [ ] `dotnet test` и `dotnet test gui/NetZapret.Gui.sln` проходят
- [ ] обе части собираются с `-warnaserror` (так собирает CI)
- [ ] логика — в `src/`, а не в окне
- [ ] в изменениях нет ссылок на подписки, `config\netzapret.json` и содержимого `runtime\`
- [ ] правленые `.cmd` — ASCII и CRLF
