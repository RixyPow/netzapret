<UserControl x:Class="NetZapret.Gui.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Backdrop}">

    <Grid Margin="32,28,32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                <TextBlock Text="Журнал" Style="{StaticResource Title}" />
                <TextBlock Style="{StaticResource Caption}" Margin="0,6,0,0"
                           TextTrimming="CharacterEllipsis"
                           Text="Что говорят движки. Здесь видно, через какой выход шло имя и что ответила труба." />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ComboBox x:Name="Source" Width="176" SelectedIndex="0"
                          SelectionChanged="OnSource" Margin="0,0,8,0">
                    <ComboBoxItem Content="sing-box" />
                    <ComboBoxItem Content="winws2" />
                    <ComboBoxItem Content="супервизор" />
                    <ComboBoxItem Content="окно" />
                </ComboBox>

                <Button x:Name="ErrorsButton" Style="{StaticResource Ghost}"
                        Content="Только ошибки" Click="OnErrors" Margin="0,0,8,0" />
                <Button Style="{StaticResource Ghost}" Content="Обновить" Click="OnReload" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" x:Name="Status" Style="{StaticResource Caption}"
                   Margin="4,18,0,10" TextWrapping="Wrap" />

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto" Padding="16,12">
                <TextBox x:Name="Text" Style="{x:Null}"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource Text}"
                         FontFamily="{DynamicResource MonoFont}" FontSize="12"
                         IsReadOnly="True" TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Disabled"
                         HorizontalScrollBarVisibility="Disabled" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
