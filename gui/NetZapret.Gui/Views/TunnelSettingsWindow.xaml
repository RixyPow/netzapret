<Window x:Class="NetZapret.Gui.Views.TunnelSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки туннеля"
        Width="620" SizeToContent="Height" MaxHeight="820"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Backdrop}"
        ResizeMode="NoResize">

    <!--
        Отдельным окном — решение владельца 21.09. Вкладка «VPN» разрослась:
        шесть карточек настроек оттесняли за нижний край список подписок,
        ради которого на неё и заходят. Разделено по тому, как часто
        открывают: на вкладке подписки, серверы и замеры, здесь — то,
        что задают однажды.

        Список в духе Happ: одна карточка на группу, строки с волосяными
        разделителями, справа состояние, пояснения под значком «i».
    -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="22,20,22,22">
        <StackPanel>
            <TextBlock Text="Настройки туннеля" Style="{StaticResource Title}" />
            <TextBlock Style="{StaticResource Caption}" Margin="0,6,0,0" TextWrapping="Wrap"
                       Text="Всё здесь применяется при следующем запуске движков. Подписки и серверы остались на вкладке «VPN»." />

            <TextBlock Text="КУДА ИДЁТ ТРАФИК" Style="{StaticResource GroupHead}" Margin="4,22,0,7" />

            <Border Style="{StaticResource SettingsGroup}">
                <StackPanel>
<!--
                        Два выключателя вместо списка режимов. Задумка
                        владельца 21.09: пять режимов пришли по одному,
                        каждый под свой случай, и объяснить разницу между
                        «всё через VPN» и «без исключений» удавалось только
                        примером. Выключатели отвечают на вопрос, который
                        человек и задаёт: что у меня сейчас работает.
                    -->
                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Десинк" VerticalAlignment="Center" />

                            <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                            Content="winws2: вмешивается в рукопожатие TLS так, что фильтр оператора не опознаёт имя. Трафик при этом идёт напрямую, своим обычным путём, и адрес остаётся домашним.&#x0a;&#x0a;Лечит блокировки по имени и не лечит отказы по стране: сайт видит тот же адрес, что и без него." />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="DesyncWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Desync" Style="{StaticResource Switch}"
                                      Click="OnDesync" VerticalAlignment="Center" />
                    </Grid>

                    <Border Style="{StaticResource RowLine}" />

                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Туннель" VerticalAlignment="Center" />

                            <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                            Content="sing-box: поднимает сетевой адаптер и уводит через него то, что названо в маршрутах. Адрес становится зарубежным, и это лечит отказы по стране.&#x0a;&#x0a;Требует подписки. Без неё поднимать нечего — останется один десинк." />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="TunnelWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Tunnel" Style="{StaticResource Switch}"
                                      Click="OnTunnel" VerticalAlignment="Center" />
                    </Grid>

                    <Border Style="{StaticResource RowLine}" />

                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Всё через туннель" VerticalAlignment="Center" />

                            <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                            Content="Как обычный VPN: в туннель уходит весь трафик, а не только названное в маршрутах.&#x0a;&#x0a;Домашняя сеть при этом остаётся снаружи всегда — роутер, принтер, сетевой диск. Завернув её в туннель, программа оборвала бы их, и это условие работы машины, а не настройка.&#x0a;&#x0a;Исключения на прямой проход сохраняются: российские сервисы через зарубежный адрес не работают вовсе." />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="AllWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="TakesAll" Style="{StaticResource Switch}"
                                      Click="OnTakesAll" VerticalAlignment="Center" />
                    </Grid>

                    <Border Style="{StaticResource RowLine}" />

                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Игнорировать исключения РФ" VerticalAlignment="Center" />

                                <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                                Content="Отменяет одно правило — список российских сетей, шестьсот восемьдесят две подсети. Они уйдут в туннель и увидят зарубежный адрес.&#x0a;&#x0a;Прочие исключения остаются: те выведены напрямую потому, что так работает лучше, а не потому, что местные.&#x0a;&#x0a;Действует только при «всё через туннель»: в выборочном режиме туда и так уходит лишь названное." />
                            </StackPanel>

                            <TextBlock x:Name="RussianLine" Style="{StaticResource Caption}"
                                       Margin="0,4,0,0" TextWrapping="Wrap" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="RussianWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Russian" Style="{StaticResource Switch}"
                                      Click="OnRussian" VerticalAlignment="Center" />
                    </Grid>

                    <Border Style="{StaticResource RowLine}" />

                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Автоподбор только по зарубежным" VerticalAlignment="Center" />

                            <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                            Content="Подбор идёт по задержке и потому всегда сползает на ближайший сервер — свой же. Через него не работает ровно то, ради чего туннель обычно и нужен: адрес остаётся отечественным, и отказ приходит тот же, что и без туннеля." />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="ForeignWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Foreign" Style="{StaticResource Switch}"
                                      Click="OnForeign" VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="КОГДА ТУННЕЛЬ НЕ РАБОТАЕТ" Style="{StaticResource GroupHead}" Margin="4,20,0,7" />

            <Border Style="{StaticResource SettingsGroup}">
                <StackPanel>
                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Обход туннеля" VerticalAlignment="Center" />

                                <ContentControl x:Name="BypassInfo" Style="{StaticResource Info}"
                                                Margin="2,0,0,0" />
                            </StackPanel>

                            <!--
                                Единственное пояснение, оставленное в строке,
                                и намеренно. Обход уводит трафик открыто,
                                и узнать об этом наведением мыши — не то же,
                                что прочитать, не ища.
                            -->
                            <TextBlock x:Name="BypassLine" Style="{StaticResource Caption}"
                                       Margin="0,4,0,0" TextWrapping="Wrap" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="BypassWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Bypass" Style="{StaticResource Switch}"
                                      Click="OnBypass" VerticalAlignment="Center" />
                    </Grid>

                    <Border Style="{StaticResource RowLine}" />

                    <Grid Margin="16,13">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Запасной выход WARP" VerticalAlignment="Center" />

                            <ContentControl Style="{StaticResource Info}" Margin="2,0,0,0"
                                            Content="Бесплатный выход Cloudflare. Подмешивается к серверам действующей подписки, а не заменяет их: пока те живы, автоподбор берёт их — они быстрее; когда лягут, останется WARP.&#x0a;&#x0a;Учётную запись движок заводит себе сам: ни почты, ни оплаты, ни ключей.&#x0a;&#x0a;Выпускает трафик в ближайшей стране, и на российском подключении это Москва. От блокировок оператора спасает, от отказов по стране — нет." />
                        </StackPanel>

                        <TextBlock Grid.Column="1" x:Name="WarpWord" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />

                        <ToggleButton Grid.Column="2" x:Name="Warp" Style="{StaticResource Switch}"
                                      Click="OnWarp" VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock x:Name="Status" Style="{StaticResource Caption}" Margin="4,16,0,0"
                       TextWrapping="Wrap" />

            <Button Style="{StaticResource Ghost}" Content="Закрыть" Click="OnClose"
                    HorizontalAlignment="Right" Margin="0,18,0,0" MinWidth="110" />
        </StackPanel>
    </ScrollViewer>
</Window>
