# Notion.
#
# notion.com — нынешний основной домен: Notion переехал на него с notion.so,
# и сайт, и вход живут там, а notion.so только перенаправляет (замер
# 26.09: notion.so → 301, notion.com → 302 на www.notion.com). Стоит
# первым, потому что проверка блокировок берёт с части первое имя.
# notion.so оставлен: по нему ходят старые ссылки и приложение.
#
# notion.site не перенесён: это раздача опубликованных страниц, отдельная
# от самого Notion, и открывается она независимо от того, работает ли вход.
notion.com
notion.so
