# RuTracker.
#
# Только основной домен. В списке Zapret их четырнадцать: rutracker.net,
# .cr, .nl, .ru, .cc, .cloud, maintracker.org и прочие. Все ведут на тот же
# трекер, и в отчёте они давали тринадцать строк, к которым нечего добавить,
# — а половина из них ещё и не отвечала, потому что зеркала приходят
# и уходят.
rutracker.org
