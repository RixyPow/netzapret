# Claude — содержимое: артефакты, превью загруженных файлов.
#
# Отдельной частью от claude.txt: ведётся своим маршрутом, и полная
# проверка блокировок иначе до него не дошла бы — она берёт с части
# два первых имени.
#
# Замер 26.09: в своём правиле «через VPN» имя получало внутренний адрес
# туннеля, и страница claude.ai — на публичном адресе, через пин, — не могла
# к нему обратиться: браузер отказывал за 14 мс, не выходя в сеть.
# Через прокси Xbox DNS 87.228.47.204 отвечает 200 — как и сам claude.ai.
# Держать его тем же способом, что claude.ai: пином и «напрямую», не VPN.
claudeusercontent.com
