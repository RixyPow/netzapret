# The Movie Database — постеры и описания сериалов.
#
# На нём держатся сайты вроде seriesgraph.com: сама страница открывается,
# а картинок нет.
#
# Проверено 2026-08-28: image.tmdb.org — это CNAME на bunny-зону
# tmdb-image-prod.b-cdn.net, и адресом оттуда приходит 127.0.0.1. Отвечает
# так не оператор: тот же ответ даёт и Cloudflare, и Google — сеть доставки
# отшивает регион на уровне DNS.
#
# Поэтому только туннель, и не ради маршрута: проксируемому имени выдаётся
# fakeip, а настоящее разрешение случается на выходе, где спрашивают уже
# не отсюда.
image.tmdb.org
themoviedb.org
b-cdn.net

# Цель CNAME, названная явно. Суффикса b-cdn.net мало: имя, на которое
# ведёт цепочка, тоже должно получить fakeip, иначе sing-box разрешит его
# сам — из России, где зона отдаёт 127.0.0.1 — и пойдёт звонить в петлю
# вместо того, чтобы отдать имя выходу.
tmdb-image-prod.b-cdn.net
