bin/
obj/

# Развёрнутая копия, из которой программа запускается — см. build.cmd
build/
*.user
*.suo
.vs/
artifacts/

# Внешние движки: качаются отдельно, в репозиторий не кладутся
tools/

# Рантайм-артефакты диспетчера
logs/
runtime/
config/rules.yaml
config/subscription.cache.json
