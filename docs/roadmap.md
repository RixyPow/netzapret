# Планы

Что впереди. Сделанное отсюда убирается, чтобы файл не врал о состоянии:
оконный интерфейс, редактор hosts, проверка обновлений, каталог адресов,
наблюдение за соединениями, выбор рецепта десинка и бесплатный WARP были
в прежних редакциях этого файла и выпущены.

---

# Чем заняться ближайшим делом

## Довести окно и убрать консоль

Решение владельца, 17.09. Консоль в поставку не идёт — `pack.cmd` публикует
только окно, — а поддерживать её приходится наравне: за вечер 16.09 правки
в `BlockCheckCommand` делались четырежды. Она же источник главной беды того
вечера: четыре расхождения консоли и окна, все в одну сторону.

**Порядок обязателен: сперва перенести, потом покрыть тестами, и только
потом удалять.** Сейчас консоль — эталон: четыре раза за вечер права
оказалась именно она. Удалив её первой, мы лишимся того единственного,
с чем можно сверить окно. Пока она жива, ею и надо сверяться.

### Что придётся перенести

Список получен обходом: какие типы библиотек консоль использует, а окно
не трогает вовсе.

| Что | Зачем | Где в консоли |
|---|---|---|
| `TunnelHealth`, `TunnelState` | жив ли туннель вообще; без этого вердикты по проксируемым именам недостоверны, и шапка отчёта обязана говорить это первой строкой | 11 употреблений |
| `TunnelReach`, `TunnelReading` | довёз бы это имя выбранный сервер; поднимает свой sing-box без TUN и прав | `ReachThroughTunnelAsync` |
| `ExitReading` | адрес и страна выхода, и шёл ли сам замер через туннель | `PrintExit` |
| `PresetMatcher` | какая секция пресета взяла бы имя — строка «секция #7 …» | `PrintRow` |
| ~~Список пинов~~ | сделано 17.09: вкладка «Файл hosts» показывает весь файл, врозь наши записи и чужие, прибивает имя вручную, снимает свои и удаляет чужие | `PrintPinned` |

### Что уйдёт вместе с консолью

* **`NetZapret.Wfp`, 984 строки.** Наблюдение через WFP — проигравшая
  альтернатива ETW, и почему она проиграла, написано в README. Окно её
  не использует ни строкой.
* **`NetZapret.Dns`, 63 строки.** `StaticHostnameSource` и
  `NullHostnameSource` — источники имён для консольного `watch`.
* **`MenuCommand`, 2907 строк.** Сам интерактивный консольный интерфейс.
  Переносить нечего: это и есть консоль.

Итого около девяти с половиной тысяч строк, которые сегодня не доходят
до пользователя.

### Чего не потерять при переносе

Консольные команды — не только интерфейс. В них лежит работа, которой
в окне нет и которая нужна: сборка конфига в `MenuCommand`, разбор
`doctor`, ответ «куда пойдёт это имя» из `where`. Прежде чем удалять файл,
стоит пройти его глазами на предмет того, что в нём написано помимо печати.

Отдельно: `AddressProfile` и `AddressServices` в настройках больше
не применяются, код лишь объявляет вслух, что прежняя настройка не
действует. Уборка консоли — уместный повод снять и это объявление.
## ~~Своё приветствие TLS~~ и ~~четыре соединения~~

Оба сделаны 17.09, одной правкой: они оказались одной задачей.

**Соединение стало одним.** `WalkAsync` проживает его целиком — TCP,
рукопожатие, ответ, — как браузер. Прежде было четыре подряд к одному адресу:
отдельно TCP, отдельно рукопожатие 1.2, отдельно 1.3, отдельно передача.
Часть площадок такую очередь отшивала, и отказ доставался четвёртому при живых
первых трёх: `whatsapp.net`, три захода из трёх, тогда как по одному то же
соединение проходило двадцать раз из двадцати. Версии теперь предлагаются обе
сразу, и в отчёте одна колонка `TLS` вместо двух — в ней та версия,
на которой сошлись.

**Приветствие собрано байтами.** `BrowserHello` пишет ClientHello по образцу
Chrome: GREASE среди шифров, расширений, групп и версий, три шифра TLS 1.3
вместо двух, `compress_certificate`, ALPS, `status_request`, добивка
до 512 байт. Рукопожатие нарочно не доводится до конца — вопрос ровно один,
принимает ли сторона такое приветствие, и ответ виден по первой же записи,
до шифрования. Довести его значило бы написать X25519, расписание ключей
и AES-GCM ради того, что уже известно.

Спрашивается только у провалившихся. Исправному имени по-прежнему хватает
одного соединения; второе открывается там, где первое уже отказало, и отвечает
на вопрос, который прежде решался догадкой:

| Что ответили браузерному | Вывод |
|---|---|
| ServerHello | отвергнуто наше приветствие — в браузере имя откроется |
| alert | сторона и правда разборчива, и это её право |
| молчание или обрыв | дело не в приветствии; признак «её настройка» снимается |

Замер, с которого всё началось: `speedtest.net` и `i.scdn.co` отвергали
приветствие SChannel и отвечали ServerHello на собранное по образцу Chrome —
по тому же маршруту, в ту же минуту.

Что осталось от прежнего раздела и остаётся верным: **проба данных так
не сможет**. Она качает настоящую страницу и требует живой сессии, поэтому
идёт системным приветствием. Починена стадия рукопожатия; `Stall`
и `GeoBlock` по-прежнему меряются тем, что даёт Windows.

Наблюдение из 0.5.4 сюда же и, похоже, про то же: один рецепт по-разному
переживают разные стеки TLS. `hostfakesplit` открывал домен для schannel
и получал отказ у Go внутри sing-box — позиции разреза считаются
от содержимого приветствия, а оно у стеков разное.
## Тесты на окно: начаты

Проект `tests/NetZapret.Gui.Tests`, 38 проверок, собирается вместе с окном
из `gui/NetZapret.Gui.sln`. Отдельно от `NetZapret.Core.Tests` намеренно:
тем ссылаться на `gui/` нельзя — они входят в `NetZapret.sln`, куда окно
не входит, — и ссылка втянула бы WPF в сборку консоли и в CI.

Что уже покрыто:

* **`TunnelConfig.BuildAsync` целиком.** Подписка отдаётся своим слушателем
  на петле, так что проверяется весь путь: `SubscriptionClient`, разбор
  base64, заголовки панели, сборка конфига. Внутри — разделитель подписки
  не становится выходом, служебный вход поднимается ровно по настройке, WARP
  добавляется к серверам, а не вместо них, путь к кэшу движка полный.
* **Каждый раздел окна создаётся без исключения**, и само окно тоже.
* **Каждое имя стиля и кисти, на которое ссылается разметка, существует.**
  Отдельной проверкой по тексту, и вот почему: создание вкладки разбирает
  только то, что разбирается сразу, а содержимое `DataTemplate` — в мгновение,
  когда в списке появится первый элемент, то есть уже у человека. Обе проверки
  испытаны нарочной опечаткой: первая её не видит, вторая видит.
* **`PresetOrder`** — раскладка, незнакомые имена, повторы, испорченный файл.
* **`SubscriptionBook`** — перенос ссылки из настроек, переезд WARP
  в выключатель, сброс выбранного сервера при смене подписки.

Осталось: `SupervisorHost`, `EngineControl`, `Engines`, `CountryTag`.

Заодно удалён тест, который ничего не проверял: он звался живым замером,
поднимал слушателя на петле, писал в сокет байты, вычитывал их обратно
и звал разборщик заголовков — тот же, что уже проверен рядом без сокетов.
## Рецепты для имён, которые пресет не открывает

Механика выбора есть, обкатана на `api.cloudflareclient.com`. Осталось
пройтись по тем, где рецепт пресета не срабатывает: `ntc.party` и прочие.
`speedtest.net` отсюда убран: замер 16.09 показал, что дело не в рецепте,
а в нашем приветствии TLS — см. раздел выше. Заодно проверка механики на втором и третьем случае.

## ~~Проверка подмены DNS~~

Сделано 20.09. `DnsSpoof` судит по двум признакам: адрес, по которому
никто не может ответить, и сертификат, выписанный не на то имя. Второй —
главный: заглушка оператора не может подтвердить чужое имя, для этого
нужен закрытый ключ владельца. Оба выясняются попутно, без единого
лишнего соединения: адреса и так получены при разрешении имени,
сертификат и так предъявлен в рукопожатии, которое проба и так проводит.

Замер на живой сети: двенадцать имён, ни одного ложного срабатывания —
включая сети доставки с шестью адресами и звёздочными сертификатами.
В обратную сторону проверено обращением к `1.1.1.1` под чужим именем.

Частные сети намеренно не считаются подменой наравне с петлёй: по
`192.168.1.10` у человека законно живёт свой NAS, и объявить его
подменой значило бы отправить чинить исправное. Для них отдельный,
осторожнее говорящий признак.

Пометка в отчёте важнее всех прочих и вытесняет их: подмена не уточняет
вердикт, а отменяет его — до настоящего сайта мы не дошли, и показывать
рядом секцию пресета значило бы звать чинить пресет там, где правится
файл hosts.

Ниже — исходное рассуждение, по которому это делалось.

Пожелание владельца, 20.09.

Блокировка не всегда доходит до пакетов. Дешевле подменить ответ DNS —
и тогда имя разрешается в чужой адрес: заглушку провайдера, `0.0.0.0`
или воронку. Снаружи это неотличимо от блокировки по SNI: сайт не
открывается, десинк не помогает, и причина ищется не там. За вечер
19.09 так и вышло с `instagram.com`: я мерил QUIC по адресу, взятому
из подменённого ответа, и получал «QUIC не доходит» — при исправном QUIC.

Отличать надо от того, что подменой не является. Один и тот же сайт
законно разрешается в разные адреса: CDN отдаёт ближайший узел, у крупных
имён адресов десятки, и расхождение двух ответов само по себе ничего
не значит. Признак должен быть не «ответы разошлись», а «этот адрес
не принадлежит сайту».

Что проверять, по возрастанию надёжности:

| Признак | Чего стоит |
|---|---|
| адрес из заведомо служебных диапазонов — `0.0.0.0`, петля, частные сети | дёшево и однозначно: настоящий сайт там не живёт |
| ответ системного разрешителя против ответа по DoH | ловит подмену, но шумит на CDN — сам по себе вердиктом быть не может |
| сертификат по адресу не выписан на это имя | самый надёжный: заглушка провайдера не может предъявить чужой сертификат |

Третий признак — тот, на котором стоит строить вердикт, и он нам почти
ничего не стоит: `BlockCheck` уже ходит на имя рукопожатием TLS, остаётся
посмотреть на предъявленный сертификат вместо того, чтобы его игнорировать.

Починка у подмены своя, и она у нас уже есть: прибить имя к настоящему
адресу в файле hosts. То есть проверка не просто назовёт беду, а приведёт
к той единственной вкладке, где она лечится, — и не пошлёт человека менять
рецепт десинка, которым подмена DNS не лечится вовсе.

**Чего избегать.** Своего разрешителя и правки системного DNS: настройки
сети мы не трогаем — это записано в CLAUDE.md и остаётся в силе. Запрос
по DoH делается своим соединением и ничего в системе не меняет.

## ~~Каталог рецептов~~

Сделано 20.09. Рецепты больше не собираются из выбранного пресета:
`RecipeCatalog` — свой перечень с устойчивыми именами, `LuaModules`
читает с диска, что движок вообще умеет, а пресет отвечает лишь на
вопрос «потянешь ли». Вкладка «Десинк» показывает каталог целиком
и называет причину по каждому недоступному, а кнопка рядом дописывает
пресету недостающую строку `--lua-init`.

Что это чинит: приём, который движок умеет, а пресет не применяет,
прежде не предлагался вовсе; и имя рецепта было именем секции пресета,
так что перестановка секций молча обесценивала сохранённый выбор.

## Пояснения — до конца в подсказки

Начато 21.09 и брошено на полпути: в маршрутах и на части карточек VPN
абзацы убраны под значок «i», а у WARP так и висят четыре строки подряд.
Владелец это заметил сразу: «описание не везде засунул в инфо».

Пройти надо все разделы, а не только те, где мешало сильнее всего.
Правило простое: на виду остаётся то, что человек обязан прочитать,
не ища, — цена необратимого действия и предупреждение о беде. Всё
остальное в подсказку.

## Настройки VPN — в отдельное окно

Решение владельца, 21.09. Вкладка разрослась: выключатель VPN, выбор
сервера, автоподбор по зарубежным, добавление подписки, обход туннеля,
WARP — и под всем этим ещё список подписок с серверами, ради которого
на вкладку и заходят. Настройки оттесняют его за нижний край.

Разделить по тому, как часто открывают. На вкладке — подписки, серверы
и замеры; в окне настроек — то, что задают однажды.

## Форматирование в подборе рецепта

При узком окне строка рецепта разваливается: длинный набор шагов
переносится по символу и встаёт узким столбцом в пять букв. Виден на
снимке владельца 21.09.

Набор шагов — моноширинный текст без пробелов, и обычный перенос ему
не подходит. Нужен либо горизонтальный свиток на строку, либо перенос
по разделителям `:` и `,`.

## Починить WARP

Возвращается в работу. История в docs/warp.md: выход по WireGuard
у части операторов проходит рукопожатие и глохнет, MASQUE регистрируется
сам, но поднимается только там, где Cloudflare доступен. Замер 20.09
показал девять отказов регистрации подряд.

Начинать с замера, а не с правок: журнал движка идёт на уровне `warn`,
и успехи в него не попадают вовсе — я на этом уже ошибся, объявив
по их отсутствию, что MASQUE не работал никогда.

## Тест на подмену DNS

`DnsSpoof` проверен двадцатью двумя проверками на выдуманных данных
и замером на живой сети, но сама связка «проба → отчёт → пометка
в окне» тестом не покрыта. Нужен тест, поднимающий свой узел
с сертификатом на чужое имя: тогда видно, что вердикт доходит
до строки отчёта, а не только рождается верным.

## Цели 0.7.0

Названы владельцем 21.09: единый конфиг маршрутизации, режимы работы,
пояснения до конца в подсказки, удаление консоли. Первые две связаны —
конфиг делает режимы производными, — и делать их надо вместе.

## Режимы: два движка вместо пяти режимов

Задумка владельца, 21.09, и она лучше нынешней.

Сейчас режим — одно поле с пятью значениями, и каждое пришло под свой
случай. На деле же за ними стоят два независимых выключателя:

| Нынешний режим | winws2 | туннель |
| --- | --- | --- |
| Выборочно | поднят | поднят |
| Только десинк | поднят | не поднимается |
| Всё через VPN кроме РФ | не поднимается | поднят |
| Всё через VPN без исключений | не поднимается | поднят |
| Выключено | нет | нет |

Отсюда вид: два прямоугольника в строку, у каждого своя кнопка запуска.
В автозапуске — выбор, что поднимать: winws2, туннель или оба.

**Двух выключателей всё же мало, и это надо сказать честно.** Последние
две строки таблицы неразличимы по движкам: в обеих поднят один туннель,
а различаются они тем, сколько в него уходит. То есть нужен ещё один
признак — охват туннеля:

* по маршрутам (нынешнее «выборочно»);
* всё, кроме выведенного напрямую;
* всё без исключений.

Владелец это и предложил: «всё через VPN можно засунуть как опцию
в настройки впн». Итого два выключателя плюс трёхпозиционный охват —
шесть сочетаний вместо пяти режимов, и каждое осмысленно.

**Сочетание, о котором придётся предупреждать.** winws2 поднят, туннель
поднят, охват «всё»: тогда до WinDivert не доходит ничего — весь трафик
забирает TUN, — и winws2 работает вхолостую. Выглядит это как двойная
защита, а на деле лишний процесс. Окно должно сказать об этом прямо,
а не позволять принять одно за другое.

**И второе, про обход.** В режиме «всё через VPN» winws2 выключен,
и если выходы лягут, обход уведёт трафик напрямую — без десинка,
то есть закрытые сайты останутся закрытыми. Сейчас в «выборочно»
их подхватывает winws2. Разница не беда, но назвать её надо:
обход в этом режиме спасает меньше.

## Единый конфиг маршрутизации

Задумка владельца, 21.09.

Файл вида «сервис или домен → маршрут»:

```yaml
discord.com: desync
linkedin.com: vpn
vk.ru: direct
```

Главное свойство — **переносимость**. Он не трогает ни пресеты winws2,
ни конфиг туннеля: это отдельно записанное «как должны ходить маршруты»,
и один и тот же файл работает и с Universal V9, и с Default v1.
Отсюда же ввоз и вывоз: такой файл можно отдать другому человеку.

Место — на странице «Маршруты», там же, где его правят.

**Что решить до того, как писать.**

*Ключи латиницей.* `direct`, `desync`, `vpn` — а не «напрямую»,
«десинк», «через VPN». Файл предназначен для обмена, и кириллица
в ключах переживает пересылку хуже; в окне при этом остаются русские
слова. «Напрямую» и `direct` — одно и то же, разница только в том,
где написано.

*Пины.* В прежней записи задачи они упоминались наравне с маршрутами,
но пин — это адрес, а не маршрут, и в строку «сервис → маршрут»
не ложится. Либо отдельный раздел файла, либо не в этом файле вовсе.
Второе честнее: пин правит системный файл, и вывозить его вместе
с маршрутами — значит обещать переносимость тому, что привязано
к машине.

*Сервисы и домены вместе.* У владельца сейчас восемьдесят восемь правил,
и почти все — на списки каталога (`config/lists/discord.txt`), а не
на отдельные имена. Файл должен понимать оба: `discord: desync` —
про весь сервис, `discord.com: desync` — про одно имя. Иначе перенос
развернёт восемьдесят восемь строк в несколько сотен.

*Перенос.* Труднее всего именно он: терять нельзя ни одного из
восьмидесяти восьми правил и ста двенадцати пинов. Перенос должен быть
обратимым и сверяемым — собрать новый файл, собрать из него прежние,
сравнить дословно.

# Свои темы оформления

Светлая и тёмная сделаны в 0.5.5, переключаются в «Ещё» без перезапуска.
Зашитых мимо темы цветов не осталось ни одного: вся палитра — одиннадцать
цветов и два шрифта, стили элементов опираются только на них и про тему
не знают вовсе.

Осталось, чтобы темы стали **чужими**:

* вынести палитру в файл рядом с настройками, чтобы её можно было заменить,
  не пересобирая программу. Формат — обычный XAML-словарь, а не свой
  выдуманный: его понимает и WPF, и любой редактор, и не придётся писать
  разборщик;
* список найденных тем в «Ещё» вместо двух кнопок;
* проверка, что тема не ломает читаемость: контраст текста к подложке
  считается арифметикой, и отвергнуть негодную пару лучше на загрузке,
  чем показать нечитаемое окно.

**Чего избегать.** Тем, меняющих не только цвета: как только в тему попадут
отступы и размеры, каждая правка разметки начнёт ломать чужие темы, и чинить
их будем мы. Цвета и шрифты — граница, за которой начинается чужая работа
и наша ответственность за неё.


---

# Что решено не делать

**Своего движка десинка.** Zapret этим занимается годами, у него живое
сообщество и обновляемые пресеты. Наша ценность в том, что мы решаем,
чему куда идти.

**Мастеров «настроим за вас».** Ни одного утверждения, которое нельзя
подтвердить замером: вся история проекта — про вердикты, оказавшиеся
неверными, и находили их только потому, что было видно, из чего они выведены.

**Индикаторов без цифр.** Состояние показывается тем, что измерено,
а не зелёной галочкой.

**Правки файла hosts без подтверждения.** Он системный, общий и переживает
удаление программы.
