# NetZapret — передача в новую сессию

Состояние на 16.09.2026. Версия в `Directory.Build.props` — **0.6.0**,
не выпущена. Последний выпуск — 0.5.8.

**Правила работы лежат в `CLAUDE.md`** и читаются автоматически каждой
сессией: выпуски, секреты, что нельзя трогать, разбор неисправностей,
пресеты, git и как отвечать. Здесь — только состояние и очередь дел.

---

## Что уже лежит в 0.6.0

Всё собрано и оттестировано (474 теста), но не выпущено.

| | |
|---|---|
| Выбор сервера подтверждается движку | кэш sing-box помнил прошлый выбор и перебивал наш |
| Движки при автозапуске | три попытки с растущим ожиданием, исход в журнал |
| Поиск переживает пересборку | раньше сбрасывался после любого действия |
| Повторный выбор «десинк» открывает окно рецептов | раньше надо было переключать туда-обратно |
| Номер сборки в заголовке | `версия 0.6.0 (7)`, только у собранного своими руками |
| Проба рецептов читает данные | раньше мерила только рукопожатие |
| Свои списки едут к движку | `config\lists` → `lists\nz-*.txt` в бандле |
| Spotify стал сервисом | был полкой «Музыка», выглядел задвоением |

---

## Первое в очереди: первый запуск

Ядро 0.6.0. Сейчас его **нет вовсе**: человек открывает окно и остаётся один
на один с одиннадцатью разделами.

Сценарий из четырёх шагов: что у вас не работает → есть ли подписка →
пробный запуск → результат. Подробности в `docs\design-brief.md`, раздел 6 —
там перечислено пять вещей, которых новичок не знает и негде узнать.

---

## Три находки агента по пресетам

### 1. `--hostlist-domains` не раскрывает поддомены — **закрыто**

Справка winws2 различает их дословно:

```
hostlist=<filename>              ... subdomains auto apply ...
hostlist-domains=<domain_list>   ; comma separated FIXED domain list
```

Агент подозревал ту же дыру в Signal, Viber, Proton и Bluesky — подозрение
верное, дыра была. Исправлено коммитом `0a41b5a`: пять секций переведены
на файловые списки, доставку делают `build.cmd` и `pack.cmd` с приставкой
`nz-` (без неё наш `discord.txt` заменил бы одноимённый файл Zapret).

Проверено: `mail.proton.me`, `chat.signal.org`, `web.archive.org` попадают
в свои секции.

**Ответ на вопрос агента — да, переключились, и именно на файлы.**

### 2. Пин в hosts выключает десинк, и проверка об этом молчит — **открыто**

Находка верна и шире, чем выглядит. В `runtime\desync-exclude.txt` сейчас
**783 имени**, и попадают туда два разных источника:

- прибитые в `hosts`;
- поставленные на «напрямую» — это добавилось в 0.5.7.

Оба уходят в `--hostlist-exclude`, и winws2 такие имена не трогает вовсе.

**Чего не хватает:** проверка блокировок об этом не говорит ни слова.
Имя, выведенное из-под десинка, проваливается — и получает вердикт «DPI
по TLS» с советом «десинк», хотя десинк к нему не применяется по нашему же
решению. Со стороны это неотличимо от неработающего рецепта, и мы на этом
уже теряли время дважды.

**Что сделать:** пометка в строке отчёта — «мимо десинка: пин» либо «мимо
десинка: напрямую». Данные все на месте: список читается из того же файла,
что отдаётся движку.

### 3. `build\` и `dist\NetZapret\` — два деплоя — **открыто**

Подтверждаю замером:

```
build\NetZapret.exe          16 сен 11:21    ← актуальный
dist\NetZapret\NetZapret.exe 15 сен 23:56    ← сутки как устарел
```

Выглядят одинаково, и спутать их легко — агент споткнулся, я тоже.
`dist\` создаёт `release.cmd` при выпуске и больше к нему не возвращается.

**Что сделать:** либо чистить `dist\` в конце `release.cmd`, либо класть
туда файл с датой выпуска и предупреждением. Первое честнее: архив уже
опубликован, распакованная копия не нужна никому.

---

## Открытые хвосты, кроме этого

**Проверка местами описывает себя, а не сеть.** Приветствие TLS у .NET
не такое, как у браузера: другой порядок шифров, нет GREASE, нет ALPN.
`twitch.tv` числился закрытым и открывался, `itch.io` числился оборванным
и работал. ALPN пробовали — стало хуже. Это самое крупное из нерешённого
по блокчеку, и до него не стоит красить строки маршрутов по его данным.

**Маршруты подтормаживают при прокрутке.** Список не виртуализируется: все
55 сервисов живут в памяти. Мешает то, что раздел целиком лежит в одном
`ScrollViewer` — он даёт вложенному списку бесконечную высоту и отключает
виртуализацию. Нужна перестройка разметки.

**Не берёт ни один рецепт:** `spotifycdn.com` (секции нет вовсе, отдаёт RST),
`i.scdn.co`, `soundcloud.com`, `sndcdn.com`, `bsky.app`, `proton.me`,
`amazon.com`, `amazonaws.com`. Подробности и шесть вопросов — в брифе,
который уходил агенту. Часть из них может отпасть сама после правки
со списками: возможно, дело было не в рецепте, а в буквальном
сопоставлении имён.

**Просил пользователь:** кнопка «открыть пресет в редакторе» на вкладке
«Десинк» — работы на двадцать минут. И обводка по блокчеку в маршрутах —
но сперва оговорка выше.

---

## Что стоит знать, прежде чем править

**Порядок секций в пресете решает всё.** winws2 отдаёт пакет первому
совпавшему профилю и дальше не смотрит. На этом мы обожглись трижды
за двое суток: голос Discord чинился секцией, до которой не доходила
очередь; пять новых секций встали ниже «Моих сайтов» и не работали;
рецепт, выбранный вручную, не находился из-за перестановки секций.

**Тесты гоняют конфиг через сам `sing-box check`.** Это уже спасло релиз:
поле `store_selected`, которым я пытался отключить запоминание выбора,
сборка 1.14 не знает и отвергает **весь конфиг** с FATAL. Без того теста
ушло бы в выпуск.

**Секции с разбором внутри не переписывать.** В пресете есть комментарии
на двадцать строк с историей замеров — в одном из них и лежала находка
про `--hostlist-domains`. Я такой затёр «покрасивее» и восстанавливал
побайтово.

**Пересобирать перед проверкой.** Код и пресеты живут в разных местах:
пресет достаточно скопировать в `build\presets`, а код требует `build.cmd`
при закрытом окне (крестик прячет в трей, а не закрывает).
