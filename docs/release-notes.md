## Подписка на Hysteria2 больше не выглядит мёртвой

Подписка из одиннадцати серверов не работала целиком: «превышен бюджет
времени» на каждом, и ни строчки о причине. Виноваты были мы — все
одиннадцать ссылок несли `obfs=salamander` и `obfs-password`, а разбор
ссылки не читал ни того, ни другого.

Сервер, ожидающий обфускацию, отбрасывает неправильно упакованные пакеты
**молча**: ни отказа, ни сброса. Снаружи это неотличимо от выключенного
сервера — отсюда и приговор всей подписке.

После правки из тех же одиннадцати заработали три, и внешний адрес сошёл
с домашнего канала на серверный.

## Проверка серверов больше не спотыкается о собственный DNS

Пробник спрашивал имя сервера у `8.8.8.8` обычным запросом, а при
работающем движке такие запросы перехватывает туннель — замер это
показал: `8.8.8.8` отвечает адресом из служебного диапазона, то есть
отвечает не он. В логе стояло «lookup failed … context deadline
exceeded», притом что сервер отзывался по TCP за семьдесят
миллисекунд.

Теперь имена разрешаются заранее и в обход системы, а в настройку
подставляется адрес — имя при этом остаётся на месте, иначе сервер
не выбрал бы сертификат.

## Мёртвый туннель называется мёртвым туннелем

Раньше проверка блокировок в такой обстановке обвиняла сайты: пятнадцать
серверов не отвечали, а в отчёте значилось полтора десятка «закрытых»
сайтов и советы менять им маршруты.

Теперь состояние туннеля выясняется **до** вердиктов и объявляется первой
строкой. Всё, что заведено в туннель, при этом честно помечается как
«про трубу, а не про сайт».

## Строка «Выход» больше не выдаёт домашний канал за туннель

Проверка спрашивала внешний адрес обычным запросом — а он уходит мимо
туннеля, потому что имя службы разрешается в настоящий адрес. Отчёт
уверенно писал «выход туннеля в России», ни разу через туннель не сходив,
и по этому выводу советовал менять исправный сервер.

Теперь рядом с адресом сказано, через что он получен, а утверждение
о стране туннеля делается, только когда туннель и вправду измерен.

## Пин поверх маршрута виден сразу

Запись в `hosts` отменяет любой наш маршрут: файл читается раньше DNS.
Правило при этом остаётся в настройках и выглядит работающим.

Так пропал Telegram — он был заведён в туннель, а пин уводил его напрямую,
где рукопожатие рвут. Проверка добросовестно ставила диагноз прибитому
адресу и ни словом не поминала отменённый маршрут.

Теперь такие случаи собраны в отдельный раздел: пин всегда выигрывает,
и держать оба смысла незачем.

## В отчёте видно, какая секция пресета взяла имя

Правится секция, до которой исполнение не доходит, — её перехватила более
ранняя по большому списку, — и увидеть это было нечем. Теперь под строкой
стоит номер, имя секции и её рецепты.

Так сразу нашлось, что `disboard.org` стоял вторым в списке Discord
и получал Discord-рецепт, с которым рукопожатие рвётся.

## Поправлены два вердикта

**«Обрыв после рукопожатия» больше не значит «только VPN».** Рассуждение
казалось верным: секция трогает лишь рукопожатие, а поток рвётся много
позже. Опровергнуто замером — `skinsrestorer.net` рвался на 13 505 байтах
из 154 921 и вылечился рецептом на ClientHello. DPI разбирает соединение
в начале, а убивает потом; сорвав опознание, отменяешь и это.

**«Даже через VPN» не говорится, пока выход домашний.** Через
отечественный выход сайт, закрывающийся от страны, отказывает точно так же
— туннель просто не пробовался.

## ChatGPT в поставке

Правил для него не было вовсе: они жили только в личном файле, и новый
пользователь не получал ничего. Добавлены вместе с двумя доменами, без
которых рабочий туннель бесполезен, — на них лежат скрипты и вложения,
и без них страница открывается пустой.

Подстановка адреса тут не помогает, и это проверено: и пин, и настоящие
адреса отвечают одинаковым отказом. Решение принимается по стране,
из которой пришёл запрос, а не по узлу, который ответил.

## Мелкое

* `image.tmdb.org` чинится подстановкой адреса. Прежний вывод «петлю
  отдают все резолверы» устарел: один отвечает петлёй, другой — живым
  шардом, и по нему картинки грузятся.
* Рядом с прибитым адресом печатается настоящий — иначе по отчёту
  не понять, устарела запись или нет.
* В строке про обрыв сказано, сколько байтов пришло из обещанных.
