# Что нового

## Новое:

**Свою тему можно собрать прямо в программе.** «Ещё» → «Оформление» → «Создать тему». Правка начинается с цветов выбранной темы, и каждая видна на окне сразу. Цвет выбирается как в Telegram: квадрат, полоса оттенка, HSL, RGB и HEX. Фон — любая картинка с диска, с укладкой, затемнением и размытием. Если какой-то цвет перестал читаться, он отмечен прямо в своей строке, а «Поправить нечитаемое» подстроит его светлоту и не тронет выбранное. Готовую тему можно выгрузить одним файлом и передать другому — он поставит её кнопкой «Загрузить из файла…».

**Матовое стекло.** У тем с картинкой фон под карточками и боковым меню размыт, а между ними остаётся чётким. Картинку, размытие и затемнение можно выключить или усилить в «Оформлении» — эти настройки общие для всех тем. Меню у таких тем получило подложку: пункты больше не теряются на картинке.

**Шрифт и размер.** Один шрифт на всю программу — в списке каждый написан собой, выбирают глазами. По умолчанию теперь Bahnschrift. Размер — масштабом всего окна, от 90 до 150 %, как в Telegram: вместе с карточками растут списки, подсказки и окна.

**Две новые темы:** серая и тёмно-синяя в цветах Telegram.

**Обновление — на «Главной».** Версия, «Проверить» и «Обновить» переехали из «Ещё»; «Обновить» на карточке новой версии ставит её на месте, а не уводит в другой раздел.

## Исправления:

**«Маршруты» больше не называют десинк «по факту напрямую».** С 0.7.3 части, у которых в пресете нет секции по имени, были подписаны «по факту напрямую» — но секции пресета по адресам продолжают с ними работать, а «напрямую» их выключает. Если вы переводили такие части на «напрямую», верните им «десинк»: у EA на этом переставали работать серверы, у Riot — через раз.

**Выпадающие списки прокручиваются**, а не вылезают за край экрана, если в них много строк.

**Карточки растягиваются на всю ширину окна.**

**Окно меньше подвисает.** «Главная» больше не перебирает процессы и не пересобирает карточки каждые две секунды.
