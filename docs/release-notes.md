# Что нового

## Новое:

**Мёртвые серверы больше не роняют всю сеть.** Движок при неотвечающей
подписке жив и трафик исправно принимает — а наружу тот не выходит; в режиме
«всё через VPN» это означало работающую программу, зелёные проверки и
отсутствующий интернет. Теперь, если выход не отвечает три проверки подряд,
трафик уводится мимо туннеля и возвращается обратно, как только выход оживёт.

Это компромисс, а не бесплатное улучшение: трафик пойдёт открыто и с домашнего
адреса. Для обхода блокировок цена приемлема — закрытый сайт просто останется
закрытым. Для скрытности нет, поэтому на вкладке «VPN» есть выключатель, а
подпись под ним называет цену прямо.

**Проверка подмены DNS.** Блокировка не всегда доходит до пакетов: дешевле
подменить ответ DNS, и тогда имя разрешается в заглушку оператора. Снаружи это
неотличимо от блокировки по имени — сайт не открывается, десинк не помогает, и
причину ищут не там.

Проверка судит по сертификату: подменённое имя нельзя подтвердить чужим
сертификатом. Такому имени она и лечение называет верное — прибить настоящий
адрес во вкладке «Файл hosts», а не править рецепт десинка, которым подмена
не лечится вовсе.

## Исправления:

**Выбор сервера с флагом страны в названии больше не отказывает.** Запрос к
движку уходил с лишней пометкой о кодировке, и тот отвечал отказом при
совершенно верном содержимом.
