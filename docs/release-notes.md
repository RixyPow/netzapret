# Что нового

## Новое:

**Автовыбор DNS.** В разделе «DNS» — строка «Автовыбор по задержке». Включите её, и программа замерит резолверы из вашего места и поставит самый быстрый; замер повторяется при каждой проверке. Выбор резолвера руками автовыбор выключает.

**Раздел «VPN» проще.** Карточка «VPN включён» убрана — туннель включается выключателем на «Главной». Выбор сервера теперь называется «Автоподбор», с подсказкой и тумблером: нажатие возвращает автоподбор, а закрепить сервер можно кнопкой «выбрать» в списке.

**Версия — в имени архива.** Выпуск скачивается файлом `NetZapret-0.8.4.zip`, а не безымянным `NetZapret.zip`: сразу видно, что у вас лежит.

## Исправления:

**Обновление видно, пока идёт.** Проценты загрузки — на кнопке «Обновить», состояние и ошибки — прямо под номером версии, ошибки выделены. Прежде всё это писалось внизу карточки, куда не смотришь.
