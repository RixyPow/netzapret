# Что нового

## Новое:

**Свои списки сайтов.** У своего домена в «Маршрутах» теперь есть ссылка «список»: она предлагает завести ему файл, под своим названием, и дописать туда другие имена того же сайта. Файл не заводится сам — окно заранее предупреждает, если домен уже есть в списке какого-то сервиса и правила будут спорить. Уберёте домен — удалится и файл. У частей сервисов та же ссылка открывает их список.

**Проверка блокировок проверяет и ваше.** Свои домены и свои списки теперь входят в проверку наравне с сервисами — всё, что добавлено в маршруты. Своё имя без адреса показывается строкой «нет адреса у имени»: это опечатка или закрывшийся сайт, и прятать его нельзя.

**Удалить найденное в hosts разом.** Во вкладке «Файл hosts» рядом с поиском — кнопка «Удалить найденные». Найдите адрес мёртвого прокси, и все записи на нём уйдут одним нажатием, а не по одной. Перед удалением один вопрос с числом и примерами, копия файла ложится рядом.

**Проверка называет виноватый пин.** Если сайт не открывается из-за старой записи в hosts, ведущей на чужой сервер, проверка так и пишет: «имя прибито в hosts к …; удалите эту запись» — и называет настоящий адрес.

**Порядок своих правил — перетаскиванием.** В «Маршрутах» → «Порядок вычисления» свои правила таскаются мышью внутри своей группы. Так решается, какое из двух правил на одно имя победит.

**Свой домен на десинке — с рецептом.** Если пресет не знает домена, выбрать «Решает пресет» больше нельзя: без рецепта десинк не сделал бы с ним ничего, а в строке стояло бы «десинк», как будто работает.

## Исправления:

**Меньше ложных тревог о подмене DNS.** Проверка сверяет сертификат с настоящим адресом сайта и больше не зовёт подменой то, как отвечает сам сайт, — googlevideo.com, xboxlive.com.

**Notion.** В список Notion добавлен notion.com — сайт давно живёт там, и маршрут части на него не действовал.

**Диагностика больше не пугает рабочим адаптером.** При работающих движках она писала «остался туннельный адаптер» и советовала удалять его в диспетчере устройств.

**Проверка больше не пишет о nflxvideo.net и sndcdn.com.** Это адреса, с которых плееры Netflix и SoundCloud ничего не берут, — строки о них выглядели поломкой при исправно работающих сервисах.
