## Серверы, которых раньше не было видно

Подписка могла показывать меньше серверов, чем в ней есть, а то и ноль —
и это выглядело как мёртвая подписка при живом счётчике трафика.

Причин было две, и обе исправлены.

**Транспорт xhttp.** Он придуман в Xray, и обычная сборка sing-box его не
знает: такие серверы отбрасывались молча. Движок в поставке заменён на сборку
с его поддержкой. На живой подписке это четыре сервера из двадцати одного,
на другой — все двенадцать.

**Формат ответа панели.** Панели отдают разное разным клиентам, а программа
представлялась чужим именем и получала готовый конфиг вместо списка ссылок:
без названий стран, с тегами `proxy`, `proxy-2` и повторами — двадцать один
вместо двенадцати. Теперь программа представляется движком, который ведёт,
и получает обычный список с флагами и названиями.

Если панель всё же отдаёт готовый конфиг, серверы достаются и из него —
но это запасной путь, а не единственный.

## Что это открывает дальше

Новый движок умеет WireGuard, MTProto и MASQUE. Ничего из этого пока
не подключено, но теперь для этого не нужно менять движок ещё раз.

## Движки видно, пока они поднимаются

На «Главной» они больше не пропадают из списка на время запуска: жёлтые
и подписаны «запускается», как остановленные — серые. Супервизор пишет своё
состояние только после того, как поднял все службы, и до тех пор список
опустошался — ровно на те десятки секунд, когда на него и смотрят.

## Архив стал тяжелее

Около 130 МБ против 90. Движок с поддержкой xhttp вдвое больше прежнего —
85 МБ против 43. Другого способа получить эти серверы нет.
