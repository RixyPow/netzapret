## Проверка блокировок

Новый пункт меню — восьмой. Отвечает на вопрос, ради которого программу
и открывают: почему не работает и что с этим делать.

Проверка открывает настоящие соединения к сайтам каждого сервиса и смотрит
не на то, что не вышло, а **как** не вышло. Обрыв на рукопожатии TLS при живом
TCP — почерк DPI, разбирающего имя сайта; такое лечится десинком. Молчание по
всем протоколам означает закрытый маршрут, и вмешиваться в рукопожатие там
не во что — остаётся VPN.

```
  twitter.com          ок    RST   RST   ок     DPI по TLS
  chatgpt.com          ок    нет   нет   нет    DPI по TLS
  i.ytimg.com          нет   —     —     ок     порт 443 закрыт
  rutor.info           нет   —     —     нет    закрыт полностью

Итог
  DPI по TLS: 5 — десинк
    sndcdn.com, whatsapp.com, chatgpt.com, twitter.com, notion.so
  порт 443 закрыт: 1 — только VPN
    i.ytimg.com

Стоит изменить
  i.ytimg.com          десинк → VPN     порт 443 закрыт
  rutor.info           десинк → VPN     закрыт полностью
```

Последний раздел — ради чего всё и затевалось: измеренное сверяется
с настроенным, и видно не только «закрыто», но и «настроено не тем способом».

Проверять надо при остановленных движках — с включённым обходом видно сеть
уже вылеченной. Программа об этом предупреждает.

Отдельно стоит сказать, чего проверка **не** утверждает. Заманчиво было
объявлять подмену DNS, но снаружи не отличить подставленный оператором адрес
от честного, до которого закрыт маршрут. Поэтому сказано ровно измеренное:
«системный резолвер даёт нерабочий адрес», и лечится это сменой резолвера
в пункте «DNS».

## Что ещё изменилось

**Меню больше не зацикливается при закрытом вводе.** Конец ввода считался
нажатой нулёвкой: программа спрашивала, что делать с движками, ответа
не получала и возвращалась к себе же — сто двадцать килобайт перерисовок
за шесть секунд и занятое ядро. Встречалось при запуске из сценария.

**Обзор состояния и журнал** переехали под общий пункт «Диагностика» —
они об одном и том же, а место в меню понадобилось проверке.

**Подсказка «Изменить» перестала предлагать нынешний маршрут.** В команде
`where` среди трёх вариантов первым шёл тот, который уже действует.

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются — их в архиве нет.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Что это такое, как поставить с нуля, почему ругается антивирус и на каких
условиях распространяется — в [README](https://github.com/RixyPow/netzapret)
и в файлах внутри архива.
