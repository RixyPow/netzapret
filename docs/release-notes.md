# Что нового

## Новое:

**Автоподбор адреса для пина.** В окне пина первой строкой — «Подобрать автоматически». Программа сама проверяет всех кандидатов: настоящий адрес сайта, свой каталог и посредников из каталога Zapret, — и для каждого имени отмечает того, кто отвечает сам и не отказывает по стране. Проверка видна целиком: по каждому имени — адрес, чей он, чем ответил и за сколько. Подбор только советует: другой адрес выбирается щелчком по строке, ненужное имя — строкой «не прибивать», а записывает кнопка «Прибить выбранное».

Подбор находит и имена, которые сайт использует помимо своего: переходит по переадресациям (голое имя → www) и берёт имена, которые браузер недавно спрашивал у DNS. Если сайт открылся пустым или без картинок, откройте его ещё раз и подберите снова. Если какому-то имени рабочий адрес не нашёлся, окно прямо говорит, что пином сайт целиком может не заработать и нужен туннель.

**Каталог адресов работает и без установленного Zapret.** С программой едет снимок рабочих записей каталога Zapret: рабочие адреса для пятисот имён шестидесяти сервисов (ChatGPT, Claude, Gemini, Spotify, Discord, Notion, x.com и другие) и посредники для автоподбора. В снимок попало только то, что ответило при проверке.

**sing-box обновлён до 1.14.1-extended.** Бесплатный WARP снова поднимается — теперь через HTTP/2, — но под нагрузкой неровен, поэтому автоподбор берёт его только запасным, когда серверы подписки не отвечают. Выбрать его вручную по-прежнему можно. Заодно ожили серверы на WebSocket, рвавшиеся на прежнем движке.

**«О программе»** на вкладке «Ещё»: ссылки на репозиторий, сообщения об ошибках, обсуждения и телеграм-канал, а также из чего собрана программа — каждый компонент с лицензией и ссылкой на исходники.

## Исправления:

**Подписки на XHTTP заработали.** Серверы с транспортом XHTTP молчали все до одного: поставщик требует свою длину набивки запросов, а программа ставила свою, и сервер отвергал каждый запрос. Теперь настройки XHTTP из подписки доходят до движка. Подписка из девяти таких серверов, целиком мёртвая, отвечает за 220–265 мс.

**Туннель больше не залипает на WARP или на «напрямую».** Движок помнил прошлый выбор сервера в своём кэше и держался его вопреки настройкам: при «авто» весь VPN-трафик мог идти через WARP, который под нагрузкой держит одно-два соединения, — отсюда вечное подключение Telegram и молчание Instagram. Теперь выбор восстанавливается при каждом запуске движка, в том числе при автозапуске и после сбоя.

**Проверка блокировок не путает проверку «вы не робот» с отказом по стране.** openai.com и claude.ai числились «сайт отказывает по стране», хотя в браузере работали: Cloudflare отвечал проверкой, которую браузер проходит сам. Теперь такие сайты — «доступен».

**«Прервать» в проверке блокировок больше не зависает.** Если уйти на другую вкладку и вернуться, раздел навсегда оставался на «Прерываю — жду…» с выключенными кнопками, хотя проверка давно закончилась. Заодно плашка «Движки работают» описывает условия, при которых снята проверка, а не нынешние: движки, поднятые посреди проверки, её больше не перекрашивают.

**Курсор в полях ввода не налезает на подсказку.** В поиске «Маршрутов», «Файла hosts» и в полях «Диагностики» курсор вставал на первую букву серой подсказки.
