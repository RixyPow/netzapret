# Что нового

## Новое:

**Новое меню в трее.** Щелчок по значку открывает карточку в стиле программы: идёт ли обход, как чувствуют себя туннель и десинк, кнопки «Запустить/Остановить» и «Открыть окно», два выхода — оставив движки работать или остановив их. Под меню размыто то, что было на экране; меню берёт цвета, шрифт и масштаб выбранной темы. Двойной щелчок по-прежнему открывает окно.

**Оформление — своим разделом.** Темы, фон, шрифт, размер и меню в трее теперь на боковой панели, а не первой карточкой в «Ещё». Для меню в трее там же две настройки: размытие под ним и плотность подложки — от сплошной до почти прозрачной.

**Отчёт для разбора одной кнопкой.** «Ещё» → «Разбор неисправностей» → «Собрать» кладёт в папку `reports` архив с журналами движков, настройками и своими маршрутами — его и прикладывают к сообщению о неполадке. Ссылки подписок, ключи серверов и имя вашей учётной записи Windows из него вычищаются.

**Программа замечает свои подвисания.** Если окно не отвечает дольше трети секунды, в журнал пишется, сколько это длилось и в каком разделе. Журнал входит в отчёт, так что такие случаи теперь можно разобрать.

## Исправления:

**«Маршруты» открываются почти втрое быстрее.** Раздел держал окно около 0,7 секунды при каждом заходе — сверка имён со списками правил шла медленно. Теперь около 0,25 секунды.

**Подписка читается, даже когда выход VPN не отвечает.** При включённом «DNS через туннель» адрес панели подписки спрашивался через тот же туннель, и если выход лежал, подписка не читалась с ошибкой «Этот хост неизвестен» — ровно тогда, когда за новыми серверами и шли. Теперь адреса панелей узнаются в обход туннеля.

**Тема применяется и при автозапуске.** Окно, открытое из трея после входа в Windows, выходило в оформлении по умолчанию, какую бы тему вы ни выбрали.
