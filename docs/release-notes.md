# Что нового

## Новое:

**Подписки с привязкой к устройству.** Панели вроде Remnawave, которые отдают серверы только знакомым устройствам, теперь отдают подписку и NetZapret: программа называет себя постоянным номером устройства, как это делают Happ и v2rayTun. Номер — отпечаток идентификатора Windows, сам идентификатор панели не уходит. Если панель ограничивает число устройств, NetZapret займёт одно место.

**Новый пресет по умолчанию — Universal V10.** Секции, подобранные вручную для Facebook, SoundCloud, Speedtest, Medium, VirusTotal и Valheim, теперь покрывают и поддомены. В V9 им доставались только сами имена, а `www.facebook.com`, картинки Facebook и SoundCloud уходили к чужим рецептам. GitHub и приложение Bluesky получили свои секции с рецептом, подобранным вручную. Новые установки сразу получают V10; у тех, кто уже запускался, выбор остаётся прежним, а V10 выбирается на вкладке «Десинк».

## Исправления:

**«Напрямую» больше не ломает то, что пресет пропускает.** Прежде имя на «напрямую» выводилось из-под десинка не целиком: его соединения могли подобрать секции пресета, работающие по адресам, и так на «напрямую» не входил Valorant (VAL 43). Теперь соединения с именами на «напрямую» и прибитыми в hosts проходят мимо десинка целиком.
