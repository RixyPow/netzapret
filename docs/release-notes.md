# Что нового

## Новое:

**Каталог рецептов десинка.** На вкладке «Десинк» появился перечень из
одиннадцати рецептов с объяснением, что каждый делает. Он больше не собирается
из выбранного пресета: выбрать прежде можно было только то, что пресет уже
применяет, — приём, который движок умеет, а пресет не использует, не
предлагался вовсе.

Каталог сразу говорит, потянет ли рецепт ваш пресет, и называет причину, если
нет. Это не придирка: winws2 на незнакомый приём отвечает «desync function does
not exist» и не поднимается вовсе, то есть один негодный рецепт оставляет без
десинка целиком. Кнопка рядом дописывает пресету недостающую строку — со
спросом.

Среди добавленных — четыре приёма против блокировки «16 КБ» на Cloudflare и
Ростелекоме: залп белыми приветствиями, лесенка по дальности, белое с двух
сторон, наложение белым именем.

**Свои домены встали в общий список сервисов.** Раньше они жили в отдельной
карточке и были поражены в правах: ни значка сайта, ни пина, ни выбора рецепта,
ни поиска. Теперь свой домен — такая же строка, как сервис из каталога, и всё
перечисленное у него есть.

## Исправления:

**Мёртвый выход подписки больше не гасит исправный движок.** Проверка прохода
трафика честно проваливалась, супервизор засчитывал это движку и перезапускал
его: восемнадцать секунд тишины, подъём в ту же мёртвую подписку, и по кругу.
Со стороны это выглядело как «туннель отваливается на пару секунд». Теперь
у проверки три исхода: движок вправду сломан — перезапуск уместен; беда выше по
течению — перезапуск не лечит ничего, и мёртвые серверы от него не оживают.

**Мёртвые серверы выводятся из автоподбора.** Движок опрашивал все наравне и мог
осесть на любом из молчащих, а осев — молчал до следующего замера. Замер был раз
в три минуты, столько же длилось молчание. Теперь перезамер раз в минуту,
а сервер, не ответивший три проверки подряд, из автоподбора выводится.
Удавшаяся проверка счёт обнуляет. Из ручного выбора такие серверы не убираются:
закрепить их — законное желание.

**Автозапуск больше не сносит движки из-за мёртвой подписки.** Он ждал, пока
здоровыми станут все службы, а на умирающей подписке этого не наступало никогда:
семьдесят пять секунд ожидания, снятие движков, подъём заново, и так трижды.
Около четырёх минут, в течение которых связь рвалась и вставала. Регрессия
0.6.2 — до неё запуск был один.

**Второй супервизор забирает присмотр, а не уходит молча.** Запуск натыкался на
живого предшественника и возвращал ошибку без единого слова в окне. Снаружи это
выглядело как «нажал, ничего не произошло».

**Без ссылки на VPN поднимается только десинк.** Прежде программа требовала
подписку и там, где обойтись можно было одним десинком.

**Стрелки сворачивания выправлены.** Подписки рисовали их одними знаками,
маршруты и хосты — другими, которых в системном шрифте нет вовсе, и Windows
подменяла их чужим. Один и тот же жест выходил на двух вкладках разного размера
и веса. Теперь стрелка — фигура и от шрифта не зависит.
