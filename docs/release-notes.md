# Что нового

## Новое:

**Программа сама сообщает о новой версии.** При запуске она тихо проверяет GitHub, и если вышла новая версия, на «Главной» появляется карточка: что в ней главного и кнопки «Что нового», «Не сейчас» и «Обновить». У пункта «Ещё» горит точка, пока обновление не поставлено. «Не сейчас» прячет карточку до следующей версии. Уведомлений Windows нет — выпуски выходят часто. Отключается выключателем «Искать обновления при запуске» в «Ещё»; прежде он ничего не делал.

**Свой домен важнее правила сервиса.** Если вписать, например, `downloads.claude.ai` «через VPN», а весь Claude стоит «напрямую», теперь сработает ваш выбор для этого имени. Прежде он молча проигрывал правилу сервиса.

**Пин больше не прячет выбор VPN.** Если выбрать «через VPN» для сервиса или домена, чьи имена прибиты в hosts, программа спросит, снять ли пин: пока он стоит, имя идёт по прибитому адресу мимо туннеля, какой маршрут ни выбери. Работает в «Маршрутах», в «Своём домене» и в окне пина.

**Ещё пять DNS.** Для туннеля добавлены NextDNS, ControlD, Quad9 без фильтра и Cloudflare Security. В обзор на вкладке «DNS» — ещё и XBOX DNS; для туннеля он не предлагается, потому что на закрытые сайты отдаёт адреса своих прокси в России.

**«Маршруты» говорят, когда десинку нечего делать.** Если ни одна секция пресета не отзывается на сервис, его маршрут подписан «по факту напрямую: пресет не чинит» — серым, а не оранжевым цветом десинка.

## Исправления:

**Одно правило для программы больше не может стереть все свои маршруты.** Путь вида `C:\Program Files\…\app.exe` записывался в файл своих правил так, что файл переставал читаться целиком — вместе со всеми маршрутами.

**Подсказки есть во всех полях ввода**, включая скрытое поле ссылки подписки, «Прибить вручную» и «Свой домен». Курсор стоит перед подсказкой, а не на её первой букве.

**Из пресета Universal V9 убрана нерабочая секция для Instagram.** Адреса Instagram закрыты целиком, и десинку там нечего поправлять; Instagram идёт через VPN.

**Файл настроек стал чище.** В `netzapret.json` больше не пишется второй, вычисляемый блок выключателей рядом с настоящими, а старые, давно не действующие настройки адресов сняты. Прежние файлы читаются как раньше.
