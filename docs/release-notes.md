## Проверка блокировок

Новый пункт меню — восьмой. Отвечает на вопрос, ради которого программу
и открывают: почему не работает и что с этим делать.

Проверка открывает настоящие соединения и смотрит не на то, что не вышло,
а **как** не вышло. Каждый столбец отвечает за свой способ вмешательства,
и лечатся они разным:

```
  Что проверяется                        Что означает отказ
  TCP     соединение на 443              порт или маршрут закрыт → VPN
  TLS1.2  рукопожатие старой версии      DPI разбирает имя сайта → десинк
  TLS1.3  рукопожатие нынешней версии    DPI разбирает имя сайта → десинк
  HTTP    обычный запрос на 80           отвечает ли хост вообще
  ДАННЫЕ  идёт ли поток после рукопожатия  поток убивают позже → VPN
```

В конце — перечень того, что стоит поменять, и предложение записать это
сразу. Записывается в пользовательский слой, тот же, что у пункта «свои
домены», и снять оттуда же.

Проверять надо при остановленных движках — с включённым обходом видно сеть
уже вылеченной. Программа предупреждает.

## Столбец ДАННЫЕ, и почему он появился

Перестал открываться `steamcommunity.com`. Проверка при этом называла его
доступным — и была права по-своему: соединение вставало, оба рукопожатия
проходили, HTTP отвечал. А сайт не открывался.

Оказалось, приходит ровно 14381 байт, и поток замирает. Три раза из трёх,
до байта. Оператор пропускает начало и убивает поток, опознав его позже
другими средствами; десинк тут бессилен — секция пресета помечена
`--payload=tls_client_hello`, то есть трогает только рукопожатие.

Отсюда вывод, стоивший этого разбора: **состоявшееся рукопожатие ничего
не доказывает**. Теперь проверка качает данные и следит за молчанием — не за
объёмом, у страниц он разный, а за паузой от последнего полученного байта.
Медленный сайт подаёт признаки жизни и проходит, убитый замолкает совсем.

Тем же почерком нашёлся `valheim.com`.

## Адрес-заглушка

У `seriesgraph.com` не грузились картинки. Они приходят с `image.tmdb.org`,
а тот — CNAME на bunny-зону, и адресом оттуда возвращается `127.0.0.1`.
Отвечает так не оператор: то же самое говорит и Cloudflare, и Google — сеть
доставки отшивает регион на уровне DNS.

Проверка теперь узнаёт такой ответ и не выдаёт чужой отказ за свой. Лечится
туннелем, и по причине, к маршрутам отношения не имеющей: проксируемому имени
выдаётся fakeip, а настоящее разрешение случается на выходе.

## Свои списки доменов

`config\lists\` — то, чего нет у Zapret. Оттуда Valheim, Pinterest и TMDB,
которых в его списках не значится вовсе, а значит десинк их не касается.

Steam разделён на части: сайт и сообщество отдельно от загрузок. Через
туннель имеет смысл вести только страницы — игры идут десятками гигабайт
и съели бы квоту подписки за вечер, а работают и без него.

## Что ещё изменилось

**Меню больше не зацикливается при закрытом вводе.** Конец ввода считался
нажатой нулёвкой: программа спрашивала, что делать с движками, ответа не
получала и возвращалась к себе же — сто двадцать килобайт перерисовок за
шесть секунд и занятое ядро. Встречалось при запуске из сценария.

**Доменный список больше не записывается как адресный.** Списки различаются
только содержимым, и по имени файла их не отличить; правило выходило пустым
и не совпадало никогда — молча.

**Окно проверки не закрывается, не дав прочитать.** Придерживается только
там, где закрылось бы само: в терминале приглашение и так остаётся.

**Обзор состояния и журнал** переехали под общий пункт «Диагностика».

**Подсказка «Изменить» перестала предлагать нынешний маршрут.**

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются — их в архиве нет. Свои списки в `config\lists\`
обновляются вместе с программой.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Что это такое, как поставить с нуля, почему ругается антивирус и на каких
условиях распространяется — в [README](https://github.com/RixyPow/netzapret)
и в файлах внутри архива.
