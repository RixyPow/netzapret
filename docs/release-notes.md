# Что нового

## Новое:

**Свои DNS.** В разделе «DNS» можно добавить свой резолвер: название, адрес и, если он умеет, имя для DoH. Перед добавлением программа его замерит: не ответивший не добавится, а тот, у кого по дороге подменяют ответы, добавится с предупреждением. Свой резолвер с DoH можно выбрать для туннеля, остальные видны в обзоре.

**Видно, через какой сервер идёт трафик.** На «Главной» в строке «Сервер» и в «Автоподборе» на вкладке VPN теперь написано, какой выход движок держит прямо сейчас, — например, «сейчас 🇵🇱 Польша». Если движок держит не то, что выбрано, об этом сказано прямо.

**VirusTotal.** Новый сервис из четырёх частей: сайт, документация, вход и API Google для входа. Вход через GitHub, Google и Microsoft ломался не на самом VirusTotal, а на адресах Google и Firebase, и каждой части нужен свой способ: окну входа — VPN, остальным — пин на живой адрес. Живые адреса уже вписаны, автоподбор их найдёт.

**Claude и Bluesky — папками из частей.** У Claude отдельная часть «Содержимое» — артефакты и превью файлов, у Bluesky — «Картинки и видео». Ломаются они отдельно от самого сайта: у Bluesky часть узлов сети доставки мертва, отсюда страница на заставке и лента без фото. Для Bluesky живые узлы вписаны для пина.

**DonationAlerts** в списке сервисов, по умолчанию напрямую: он не заблокирован, а десинк мог мешать виджетам стрима.

## Исправления:

**Сайты через VPN больше не ломаются после перезапуска движков.** Windows до десяти минут помнила старые адреса туннеля, а после перезапуска туннель раздаёт их другим сайтам — соединение уходило не туда: сброс, пустая страница, чужой сертификат. Теперь при запуске и остановке движков кэш DNS сбрасывается.

**Пины не снимаются случайно.** Повторное нажатие кнопки пина раньше молча снимало все пины сервиса; теперь программа спрашивает и показывает, какие имена уйдут.

**Автоподбор пина объясняет неудачу.** Если адрес не нашёлся, в журнале видно, кто и почему отказал и были ли подняты движки.
