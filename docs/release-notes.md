## Бесплатный WARP от Cloudflare

В разделе «VPN» появился выключатель «Бесплатный WARP». Первое включение
заводит ключи и регистрирует их в Cloudflare — ни почты, ни оплаты, ни
согласия на рассылку, пара секунд.

Включённый, он **добавляется** к серверам действующей подписки, а не заменяет
её. Пока те живы, автоподбор берёт их — они быстрее; когда лягут, останется он.
Ради этого всё и затевалось: сейчас в таком случае не остаётся ничего.

Выключателем, а не строкой в списке подписок, — и это исправление на ходу.
Строкой он был вреден: конфиг движка собирается по одной ссылке, и выбор его
выхода делал действующим **его**, отключая рабочую подписку целиком. Запасной
выход занимал место основного вместо того, чтобы его страховать.

**Выпускает трафик в ближайшей стране** — на российском подключении это Москва.
От блокировок оператора спасает, от отказов по стране нет: адрес остаётся
отечественным. Замер 2026-09-11: `loc=RU`, `colo=DME`.

Заводится он и без работающего туннеля, и это было главным в задаче. Узел
Cloudflare, на котором регистрируются клиенты, у российских операторов закрыт
по имени в TLS: соединение устанавливается, рукопожатие не доходит. Но закрыт
не весь домен — только одно имя из него, а рядом живёт второе, обслуживающее
тот же путь. Программа спрашивает сперва его. Требовать для регистрации
поднятого туннеля значило бы выдавать зонт только в сухую погоду.

### Чего ждать честно

**У нас туннель WARP не заработал.** Рукопожатие WireGuard проходит,
а дальше молчание: данные не возвращаются ни на порту 2408, ни на 500, 4500,
1701 и 908. Так выглядит либо отбрасывание пакетов WireGuard оператором после
рукопожатия — частая в России картина, — либо отсутствие поля `reserved`,
которое Cloudflare кладёт в каждый пакет, а sing-box 1.14 из настройки пира
убрал.

Регистрация при этом работает, запись сохраняется, конфиг собирается,
и на сетях, где WireGuard не трогают, всё это должно работать как обычный
сервер. Замерьте его кнопкой «Замерить все» — если показывает «не отвечает»,
значит здесь тот же случай.

## Второй выход WARP: MASQUE

Тот же WARP, но поверх QUIC на 443 порту — оператору он неотличим от обычного
веба, и там, где WireGuard глохнет, у него есть шанс. Появляется в списке
серверов рядом с первым; какой из двух работает, покажет автоподбор — он
опрашивает обоих.

Ключей он не требует вовсе: движок в поставке несёт готовый клиент Cloudflare
и заводит себе запись сам, а хранит её в собственном кэше, который теперь
включается вместе с ним.

**И он работает.** Замер 2026-09-11 на российском подключении: регистрация
прошла, туннель поднялся, `warp=on`. Причём мимо нашего же туннеля —
с привязкой к физическому адаптеру, то есть ровно так, как будет в бою.

**Оговорка.** Направить эту регистрацию нечем: ни `detour` у самого выхода,
ни `route.default_http_client` на неё не влияют — с заведомо мёртвым прокси
приходит та же ошибка через те же десять секунд, что и без него. Запрос
уходит напрямую мимо любых настроек, и MASQUE поднимется там и только там,
где `api.cloudflareclient.com` открыт. Утром того же дня он был закрыт,
к вечеру открылся — похоже, стараниями десинка, но это пока не разделено
чисто. Если у вас не поднимется, первое, что стоит проверить, — работает ли
десинк.

Замер его не трогает и подписывает «только в работе»: пробник поднимает свой
движок, а запись лежит в кэше работающего, и файл занят им же. Отказ вышел бы
не измерением, а особенностью замера.

## Наблюдение

Новый раздел. Показывает соединения по мере появления и правило, которое
к каждому применилось бы: время, куда пошло, чей процесс, какое назначение,
какое правило сработало. Ничего не перенаправляет и ни на что не влияет.

Отвечает на вопрос, на который не отвечало ничто: «почему это пошло не туда».
Маршруты показывают, как правила записаны, проверка блокировок — что закрыто
снаружи, а здесь видно, какое правило досталось настоящему соединению
настоящей программы. Расхождение между первым и третьим и есть та ошибка,
которую иначе ищут наугад.

Это была последняя команда консоли без пары в окне.

## Разрезанное приветствие для собственных запросов

Программа впервые применила к себе то, чем занимается: запрос к Cloudflare
уходит с приветствием TLS, разрезанным посередине имени. Половины проходят
проверку по отдельности, а сервер собирает их обратно сам.

Приём слабее того, что делает winws2 с полным набором рецептов, и на месте
второго имени он не понадобился. Останется на случай, когда закроют и его.

## Источник движка назван

В `docs/THIRD-PARTY.md` вписана ссылка на репозиторий сборки, которую мы везём.
Раньше там стоял только исходный проект, а сборка чужая и изменённая — GPL
требует называть источник именно её.
