# Что нового

## Новое:

**Свои темы оформления.** Тема — папка в `themes\` рядом с программой: цвета, шрифты и картинка фона. Выбирается в «Ещё» → «Оформление» плитками, как в Telegram, и применяется сразу, без перезапуска. Кнопка «Папка тем» открывает, куда их класть; формат описан в `themes\README.md`, и начать проще всего с копии встроенной тёмной или светлой. Тема, в которой текст не читается, не применяется: программа назовёт, какой цвет на каком фоне и насколько. С картинкой фона текст проверяется по её самому светлому и самому тёмному месту.

**Одно поле в «Маршрутах».** Поиск и «Свой домен» теперь одно поле. Вписанное ищется по сервисам и доменам; если такого сайта в списке нет, программа предложит его добавить. Добавленный сразу стоит «напрямую», а режим, рецепт и пин меняются прямо в его строке.

**Повторы в файле hosts.** Во вкладке «Файл hosts» появилась карточка про имена, прибитые больше одного раза. Повторы с тем же адресом убираются одной кнопкой — работать всё будет как прежде, Windows берёт первую запись. Повторы с другим адресом показываются отдельно: сейчас они не действуют, но заработают, если снять запись выше.

**Окно аккуратнее.** Боковое меню разделено: сверху разделы на каждый день, ниже «Разбор» — проверка, DNS, hosts, наблюдение, журнал и диагностика. Полоса прокрутки больше не белая, а в цветах темы. Углы скруглены мягче, рамки плотнее.

## Исправления:

**Сайты с общим пином больше не уходят в VPN из-за одного имени.** Если несколько имён прибиты в hosts к одному адресу, а «через VPN» стоит только одно из них, прежде в туннель уводился адрес целиком — со всеми остальными именами. Так с поднятыми движками переставал работать Claude. Теперь в VPN идёт только то имя, для которого это выбрано.

**Файлы программы можно удалить после остановки.** Драйвер перехвата оставался в системе и держал `Monkey64.sys`, пока не перезагрузишь компьютер, — а сценарий удаления пытался остановить его под неверным именем. Теперь драйвер выгружается при остановке движков, если им больше не пользуется ни наш движок, ни Zapret GUI, и `uninstall.cmd` останавливает его правильно.

**Светлая тема читается лучше.** Подпись на зелёной кнопке и жёлтые «внимание» стали контрастнее: три цвета не проходили ту же проверку, что теперь требуется от любой темы.
