## Пять игровых пресетов

`Default v1`–`v5 (game filter)` из поставки Zapret. Раньше их не было видно
по двум причинам сразу: программа читала только каталог `presets\winws2`,
а сборка вдобавок **удаляла** `winws2_builtin` целиком. То есть научить
программу их читать было мало — до собранной копии они всё равно не доезжали.

Взяты поимённо, а не всей папкой: там больше сотни файлов, почти все —
переборные варианты одной стратегии, и они утопили бы те два десятка,
между которыми выбирают.

Заодно: пресеты с одинаковым заголовком теперь различимы. Свой
`Universal V5 my.txt` объявляет себя тем же «Universal V5», что и заводской,
и в списке они выглядели двумя одинаковыми строками.

## Отказ сайта по стране

ChatGPT не удавалось перевести на десинк, и дело было не в десинке.
На настоящем адресе рукопожатие проходит за семь сотых секунды и страница
приходит — 403 от Cloudflare с московского узла, «недоступно в вашей стране».

Все прежние мерки называли это «доступен» и по-своему были правы: работала
связь, отказывал сайт. Десинк влияет на то, как соединение выглядит для DPI,
а решение принимает сайт, уже разобрав запрос. Помогает только выход
в другой стране.

Считаются только 403 и 451. Прочие коды говорят о странице, а не о стране:
404 и 500 на корневом пути встречаются у совершенно здоровых сайтов.

## Оборванное имя и подстановка адреса

`tr.rbxcdn.com`, по которому Roblox отдаёт превью, — это CNAME на
`trns1.rbxcdn.com`, у которого записи A нет ни у одного резолвера.
Соединяться не с чем: ни десинк, ни туннель не помогают.

Проверка теперь отличает такое от безобидного случая вроде `akamai.net`:
имя с CNAME — то, на которое ссылаются, и оборванная цепочка у него поломка,
а имя без CNAME и без адреса — просто маркер зоны.

Лечится файлом **`config\addresses.yaml`**: адрес подставляет наш резолвер,
системный `hosts` не трогается. Разница существенная — правки в hosts требуют
администратора и переживают удаление программы.

## Отчёт стало с чем сравнивать

Проверка снимает не свойства сети, а поведение сети под конкретной настройкой.
Два прогона под разными пресетами дают разные строки, и оба выглядят одинаково
достоверно. Теперь отчёт открывается тем, чем он снят:

```
Снято 29.08.2026 21:50
  Режим:   выборочно
  Пресет:  Universal V6
  Сервер:  🇺🇸 Hysteria2 | США (вход РФ)
  DNS:     8.8.8.8
```

Сервер спрашивается у движка, а не берётся из настроек: там «авто»,
а работает в этот момент один определённый.

**Метка «чз»** — цель уже идёт через туннель, и её строка описывает туннель,
а не сеть. Столбец TCP там всегда «ок», потому что соединение принимает
sing-box, ещё не дозвонившись до хоста.

## Подбор маршрута перестал спорить сам с собой

`image.tmdb.org` шёл через VPN, оттуда приходил обрыв, проверка читала это
как «DPI по TLS» и предлагала спуститься на десинк — одновременно с разделом
«маршрутом не лечится» про тот же домен.

Вердикт снимается на том маршруте, который в силе, и переносить его на другую
ступень нельзя. Пока движки работают, лесенка только поднимается.

Добавлен раздел **«Маршрутом не лечится»** — случаи, где перепробовано всё.
Молчать о них хуже: молчание читается как «средство не заметило».

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются — их в архиве нет.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Что это такое, как поставить с нуля, почему ругается антивирус и на каких
условиях распространяется — в [README](https://github.com/RixyPow/netzapret)
и в файлах внутри архива.
