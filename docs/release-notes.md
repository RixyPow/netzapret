## Адрес проверяется до того, как попадёт в hosts

Закрепление брало первый непустой ответ резолвера и писало его в системный
файл. На `image.tmdb.org` таким ответом оказался адрес, который **вообще
не отвечает**, — тогда как три других шарда той же сети отдавали картинку
с кодом 200. Какой достанется, зависит от того, кого спросили, а у сети
доставки шардов десятки.

Теперь собираются **все** кандидаты со всех резолверов, и каждый пробуется
по-настоящему: рукопожатие с нужным именем, затем запрос. Открытый порт
здесь ничего не доказывает — у пустых шардов он тоже открыт.

```
  Спрашиваю адреса и проверяю каждый: имён 2…
    image.tmdb.org                 138.199.37.227 (из 4 проверенных)
    tmdb-image-prod.b-cdn.net      ни один из 2 не отвечает
```

В файл попадает только то, что ответило.

## Протухшие пины находятся и заменяются

Пин стареет молча, и в этом его главная беда. Адрес сети доставки живёт
днями, чужой прокси — пока его содержат; когда узел умолкает, прибитое имя
перестаёт открываться, а в файле стоит уверенная строка. Со стороны это
выглядит новой блокировкой, и чинить начинают не с того конца — так вышло
с LinkedIn, TikTok и TMDB, каждый раз заново.

Пункт **«з»** в разделе «Файл hosts» проверяет свои пины и предлагает
замену, найденную тем же перебором. Чужие записи не трогаются: подменить
чужой адрес своим — то же, что стереть чужую строку.

## Петля — не ответ

С работающими движками мы сами закрываем DoH к системным резолверам,
и запрос к `1.1.1.1` уходит к тому, кто отвечает `127.0.0.1` по
`image.tmdb.org`. Заглушка честно отбрасывалась, и закрепление сообщало
«набор не покрывает ни одного имени» — при том что честный ответ был
этажом выше.

Резолвер теперь идёт дальше мимо заглушки, а не останавливается на первом
успехе, и первым спрашивается тот адрес, что задан именем, а не числом.

## Обновление больше не сотрёт ваш каталог

`config/catalog.yaml` не входил в список сохраняемых — обновление затёрло
бы ровно тот файл, в который мы зовём дописывать найденные адреса.

Цена у сохранения есть: записи, добавленные в каталог нами, до уже
обновившихся не дойдут — их файл останется своим. Из двух зол это меньшее.

## Отчёт стал короче и честнее

* **Мнимым отказам больше не даются советы.** Сказать «может быть
  неправдой» и тут же предложить починку — значит вернуть человеку выбор,
  ничего не добавив. `disboard.org` и обновления Discord из «Стоит
  изменить» исчезли.
* **Десять пустых зон** убраны из проверки: `cdninstagram.com`,
  `akamai.net`, `cloudfront.net` и прочие. Записи A у них нет и не было —
  работают только поддомены, а зона существует ради правил по суффиксу.
* **Ещё два зеркала** не проверяются: `rutracker.cr`, `notion.so`.
* `itch.io` и `tiktok.com` помечены как склонные к ложной тревоге.

## Настройка, которую нельзя было изменить

Проверка обновлений при запуске действовала, а переключить её было нечем —
ни строки в меню, только правка `netzapret.json` руками. Та же болезнь,
что и мёртвый пункт меню, вид сзади. Теперь состояние названо рядом
с пунктом «Обновление», и есть переключатель.

Раздел «Ещё» проверен целиком: мёртвых настроек в нём нет.

## Снятие пина спрашивает

Пин ставят не от хорошей жизни. Если прибитый адрес всё ещё отвечает,
снятие предупредит: имя вернётся обычному резолверу — тому самому,
из-за которого пин и понадобился. Мёртвый снимается молча.
