## Каталог сервисов Zapret

Адреса, которые люди вписывают в hosts, лежат в каталоге, поставляемом
с Zapret: семь наборов чужих прокси, отвечающих на одни и те же восемьсот
имён. Именно так и появляются «670 записей на один адрес» — это набор,
переписанный в системный файл, и когда узел за адресом умолкает, разом
перестают работать все имена сразу.

**DNS → Каталог Zapret** читает тот же каталог и отдаёт те же ответы **через
наш резолвер**. Разница в этом и состоит: не нужен администратор, ничего
не переживает удаление программы, и на приложения, которые мы не ведём,
это не действует. Снять — удалить строку из нашего конфига, а не править
системный файл.

Сервисы включаются поимённо, а не все разом: выдать адрес чужого прокси тому,
что и так работает, значит увести рабочий трафик через посторонний узел.
Наборы тоже выбираются вручную — лучшего среди них нет, каждый живёт ровно
столько, сколько живёт узел за ним, и если сервис перестал открываться,
чаще помогает другой набор, а не другой режим.

## Пресеты стали своими

Теперь они лежат в папке `presets` рядом с программой, и только там. Установка
Zapret больше не читается: у неё две папки и полторы сотни файлов, почти все —
переборные варианты одной стратегии, и решать за человека, какие показать,
приходилось нам. Вдобавок набор менялся под нами при каждом обновлении Zapret.

Положенный в папку файл появляется в списке при следующем заходе в меню —
перезапуск не нужен. Открыть папку: **Пресет Zapret → п**.

Списки доменов по-прежнему берутся у Zapret: пресет ссылается на них
относительно корня установки, и он остаётся рабочим каталогом при запуске
winws2.

## Автовыбор сервера больше не сидит дома

ChatGPT не открывался через туннель, и причина оказалась не в блокировке:
автовыбор «по задержке» раз за разом брал российский сервер. Иначе и не мог —
меньше всего задержка у ближайшего, а ближайший свой. Туннель выходил
российским адресом, и OpenAI отказывал ровно так же, как без туннеля.

Отечественные серверы выведены из автоподбора. Выбрать такой вручную
по-прежнему можно; переключатель — в пункте «VPN».

Отсев идёт по названию и потому приблизителен: сервер, названный «США
(вход РФ)», по замерам выходит в России, и по имени этого не увидеть. Поэтому
проверка блокировок теперь спрашивает страну выхода отдельно и говорит, если
та не та.

## Что ещё изменилось

**Пин в hosts виден там, где выбирается маршрут.** «ChatGPT: VPN» было правдой
о правиле и неправдой о трафике: пока имя прибито, режим ни на что не влияет.
Теперь запись показана над выбором, и её можно снять тут же.

**Проверка блокировок перестала зависать.** Сторож клавиши Esc продолжал
работать после проб и забирал ответы у следующих вопросов — со стороны это
выглядело зависанием, которое проходит, если долго жать на клавиши.

**Подставленные адреса дошли до дела.** Конфиг собирается в двух местах,
и меню не передавало их вовсе: файл лежал, адрес в нём был, а в конфиг
не попадал.

**Enter больше не приходится нажимать дважды** — в семи местах сообщение
и пауза дублировали друг друга.

**Все свои маршруты можно удалить разом** — двумя минусами в списке своих
доменов.

**Файл удаления** `uninstall.cmd` в архиве: снимает задачу автозапуска, службу
драйвера и исключение Defender. Папку с настройками не трогает.

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются. Обновиться можно и из самой программы: **Ещё →
Обновление**.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Что это такое, как поставить с нуля, почему ругается антивирус и на каких
условиях распространяется — в [README](https://github.com/RixyPow/netzapret)
и в файлах внутри архива.
