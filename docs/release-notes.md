# Что нового

## Новое:

**Режимы работают строго по маршрутам.** Куда пойдёт каждый маршрут, теперь зависит от того, что включено:

| Включено | «напрямую» | «десинк» | «через VPN» |
|---|---|---|---|
| только десинк | напрямую | десинк | напрямую |
| десинк и туннель | напрямую | десинк | VPN |
| только туннель | напрямую | VPN | VPN |
| «игнорировать исключения» | VPN | VPN | VPN |

**«Игнорировать исключения» — всё через туннель.** Прежняя настройка «Отечественные сайты — тоже через туннель» снимала только список российских сетей. Теперь она отправляет в туннель всё, включая «напрямую» и «десинк»; снаружи остаётся только домашняя сеть. Десинку при этом чинить нечего, и он не поднимается, даже если включён, — главная говорит об этом прямо. Ваш прежний выбор сохраняется.

**Обзор резолверов** в разделе «DNS». По каждому из пятнадцати провайдеров — задержка по DoH, DoT и обычному DNS, кто на самом деле отвечает на обычный запрос и подменяет ли он ответы. Видно то, чего не видно по задержке: например, что на запросы к Google и Cloudflare по UDP отвечает российский резолвер и разрешает закрытые сайты не туда. Замер идёт мимо туннеля, так что работает и при запущенных движках.

**Одиннадцать DNS для туннеля вместо шести:** добавлены Yandex, Comss.one, CleanBrowsing, Alibaba и Mullvad. Задержку в списке показывает тот же обзор, кнопка «Проверить» на вкладке теперь одна.

**Проверка блокировок называет лечение.** В итоге у каждого вида блокировки — чем он лечится: десинком, другим рецептом, только VPN. А если сайт не открывается из-за того, что системный DNS даёт ему нерабочий адрес, об этом говорит отдельный раздел — лечится это сменой резолвера, а не маршрутом.

**Пин проверяет, не откатил ли его антивирус.** Если защитник сразу вернул файл hosts к умолчанию, окно так и скажет — вместо «прибито» про запись, которой уже нет.

## Исправления:

**Instagram и другие сайты через VPN снова открываются.** Десинк портил их соединения уже внутри туннеля: если на имя в пресете стоял рецепт с поддельными пакетами, рукопожатие рвалось. Теперь сайты «через VPN» из-под десинка выведены.

**Подписка больше не показывает ноль серверов.** Часть панелей отдаёт подписку готовым конфигом sing-box, и такой ответ разбирался в пустоту, хотя срок и лимит приходили. Теперь серверы из него читаются: VLESS с REALITY, Trojan, Hysteria2 с обфускацией, Shadowsocks, VMess.

**Программа не просит `rules.yaml` при запуске от администратора.** Windows делает рабочей папкой `System32`, где тоже есть папка `config`, и программа принимала её за свою. Теперь она узнаёт свою папку по файлу правил.

**«Напрямую» работает и при выключенном туннеле.** С одним десинком winws2 получал вчерашний список исключений или никакого, и сайт на «напрямую» — например, Twitch — всё равно проходил через десинк.

**Движки не падают вместе с надзором.** Если окно читало файл состояния ровно в тот миг, когда надзор его обновлял, надзор падал и уводил с собой движки. Теперь он это переживает.

**Непрочитанная подписка называет причину.** «Не разобралась», «панель не ответила за 12 с» или «чтение прервано» — вместо невнятного «The operation was canceled». Сбои пишутся в журнал, ссылка — нет.

**Заглушки из hosts больше не уходят в туннель.** Строка вида `127.0.0.1 сайт` у сайта «через VPN» заводила петлю машины в туннель.

**Список маршрутов больше не зажат.** Карточка книги маршрутов отнимала у списка сервисов почти всю высоту; теперь «Сохранить» и «Загрузить» стоят в одной строке с «Порядком вычисления».

**Остатки прошлого обновления убираются** при следующем запуске, а устаревшие замеры серверов — после чтения подписок.
