## Пин в hosts стал маршрутом, а не борьбой с маршрутом

Четвёртый способ направить сервис, рядом с «напрямую», десинком и VPN:

```
  ChatGPT: сейчас VPN
  Список: lists/chatgpt.txt (4 доменов, например chatgpt.com)

  1. Напрямую
  2. Десинк
  3. VPN
  4. Закрепить пин в hosts
  5. Убрать свой выбор — вернуть как было
  0. Оставить как есть
```

Нужен там, где мешает не канал, а ответ: сайт закрывается от страны либо
резолвер отдаёт заглушку. Адрес берётся из наборов каталога Zapret — семи
списков чужих публичных прокси — или у честного резолвера по DoH.

Закрепление **переводит сервис на «напрямую»**, и это не довесок, а условие
работы. Почему — ниже.

## Пин не отменяет маршрут, и это было сказано неверно

Здесь и в программе стояло: пока запись в hosts на месте, маршрут ничего
не решает. Утверждение оказалось ложным, и стоило оно дня разбирательств.

Пин задаёт адрес. Дорогу задаёт правило — а имя TUN узнаёт из рукопожатия,
и правило по домену срабатывает поверх прибитого адреса.

Разошлось на живом случае. Один и тот же прибитый адрес отдавал сертификат
и настоящий ответ на `deepl.com`, `strava.com` и `jetbrains.com` — правил
у них нет, — и молчал на `chatgpt.com`, у которого правило `proxy` уводило
соединение в туннель, а туннель в тот час не работал. Адрес был жив
и числился мёртвым; чинили не то.

Отсюда правило: **пин и правило `proxy` на одном имени — всегда ошибка
настройки.** Проверка блокировок выносит такие случаи отдельным разделом
и говорит, кто из двоих выигрывает.

## Как правится файл

Осторожность здесь не формальность: файл системный, общий для всех программ,
и ведём его не только мы.

* Свои записи — в блоке `# >>> netzapret begin >>>`, и он ставится **выше**
  остальных: при двух строках на одно имя читается первая.
* **Чужие строки не трогаются никогда.** Редактор hosts есть и в Zapret GUI,
  и часть записей может быть нужна для обхода. Если на то же имя есть чужая
  запись, о ней сообщается, а не удаляется — иначе снятие нашего пина
  выглядело бы несработавшим, когда всплывёт её адрес.
* Перед каждой записью — копия рядом с файлом, следом `ipconfig /flushdns`.
* Пункт «Файл hosts» научился **открывать файл**: для адреса, которого нет
  ни в одном наборе, дописать его больше негде.

## Поставляется способ найти адрес, а не сам адрес

Обе встроенные подстановки оказались числами с недолгим сроком годности,
и обе успели протухнуть.

`tr.rbxcdn.com` стоял как имя без собственного адреса. Замер показал, что
Roblox перевёл имя на другую сеть доставки и цепочка разрешается до конца:
наша строка перестала помогать и стала замораживать прежний узел вместо
рабочего.

`image.tmdb.org` регион по-прежнему отшивает, но живой узел сменился трижды
за четверо суток. Число, попавшее в релиз, устареет раньше, чем релиз
скачают. Устойчив не адрес, а то, что один резолвер врёт, а другой нет.

Поэтому `config\addresses.yaml` поставляется пустым, а адрес берётся
на вашей машине в момент закрепления — вариантом «спросить честный резолвер
сейчас». Ответ-петля при этом отбрасывается, а не записывается: это и есть
та неисправность, которую чиним.

Вариант не годится против отказа по стране — там честный адрес самый
настоящий, и отказ придёт с него же. Для таких сервисов нужен набор
из каталога.

## Мелкое

* Раздел «пин против маршрута» в проверке блокировок предлагает оба выхода,
  а не один: снять пин ради туннеля либо поставить «напрямую» ради пина.
* Подпись к превью Roblox больше не утверждает поломку, которую починили
  на той стороне.
