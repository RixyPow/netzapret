## Все списки теперь наши

Программа ссылалась на файлы установки Zapret — `lists/discord.txt`,
`lists/whatsapp.txt`, `lists/ipset-ru.txt`. Выходило это боком дважды.

Правило на такой файл без установки Zapret **не совпадает ни с чем**,
и молча: список загружается пустым, маршрут достаётся умолчанию, а в меню
часть по-прежнему числится направленной. Для правила «российские сети
напрямую» это означало весь домашний трафик в туннеле.

И состав задавал не тот, кто отвечает за последствия. В `discord.txt` лежало
два десятка чужих каталогов серверов — `disboard.org`, `top.gg`, `mee6.xyz`;
в `roblox.txt` — зоны Akamai и Amazon целиком, десяток опечаточных доменов
и два десятка страновых зеркал; в `soundcloud.txt` — четыре сервера имён AWS,
которые отвечают по UDP 53 и в проверке по 443 числились закрытыми всегда.

Сорок три списка переехали в `config/lists/`. Основной домен первым, зеркала
и чужое убраны. Старые ссылки понимаются по-прежнему: `lists/whatsapp.txt`
читается как наш путь, а ссылка на список, которого мы не ведём, не трогается.

Заодно починены две молчаливые ошибки. У Discord второй прокси картинок
записан у Zapret как `mages-ext-2.discordapp.net` — без первой буквы, — и не
совпадал ни с чем. У X и GitHub картинки и raw-файлы лежат на `twimg.com`
и `githubusercontent.com`, вынесенных отдельными файлами, на которые
не ссылалась ни одна часть каталога.

Целей в полной проверке стало 43 вместо 71, и это не потеря охвата,
а вычтенный шум.

## Проверка больше не топит сама себя

`whatsapp.net` в прогоне на семьдесят одну цель выходил «закрыт полностью»,
а он же в прогоне на четыре — «доступен». Сеть та же, имя то же; разница
в том, сколько проб шло рядом. Последняя проба каждой цели тянет до четверти
мегабайта, и восемь таких закачек разом съедают полосу, в которой соседи
ждут ответа свои четыре секунды.

Одновременность снижена вдвое. Но одной цифрой это не лечится — сеть у всех
разная, — поэтому отказы, которые теснота могла подделать, теперь
переспрашиваются **поодиночке, в тишине**, и результат печатается отдельно:
«было „закрыт полностью“, стало „резолвер даёт нерабочий адрес“».

Переспрашивается только то, что ставится по неполученному ответу. Заглушку
в DNS и отказ по стране повторять незачем: они получены по ответу, который
пришёл. Приговор при этом не повышается — неудача второго захода остаётся
неудачей второго захода.

## Отказ называется своим именем и адресом

Было «нет ответа за 4 с». Стало «нет ответа за 4 с, 157.240.205.60:443 —
всего адресов 5», а отказ сокета вдобавок называется кодом и словом:
«соединение отвергнуто, ConnectionRefused (10061)».

Без адреса строка говорит о хосте, а отвечал за неё, случалось, прибитый
в hosts адрес или fakeip нашего же туннеля — и человек чинил сайт, тогда как
чинить надо было запись в файле.

## WhatsApp: виноват был резолвер

Три имени WhatsApp системный DNS разрешал в `157.240.205.60`, куда соединение
не встаёт вовсе. Честный резолвер отдаёт `57.144.249.32`, и тот отвечает
за десятые доли секунды. Соседний адрес той же сети, `157.240.205.53`, тоже
жив — закрыт ровно тот, который выдают.

Проверка называла это «закрыт полностью → только VPN» и тут же, отдельным
разделом, советовала сменить DNS. Теперь «не ответило ничего» разбирается
на два случая: закрыт маршрут — нужен туннель; молчит именно выданный
адрес — нужен другой резолвер. Второй проверяется одним запросом к DoH
и одним соединением, только там, где всё остальное уже провалилось.

Отказ резолвера убран из раздела «маршрутом не лечится»: маршрут там ни при
чём, а два одинаково уверенных совета рядом — хуже одного.

## Правила, спорящие об одном имени

Новый раздел, и он сразу нашёл поломку, жившую месяцами. В базовом наборе
четыре правила ведут ChatGPT в туннель; в пользовательском слое одна строка
на список `chatgpt.txt` ведёт те же четыре имени напрямую. Пользовательский
слой проверяется раньше — заводские правила мертвы, и узнать об этом было
неоткуда: в файле есть, в меню значатся, не работают.

Спором считается только расхождение в маршруте. Правило на зону и правило
на её поддомен с одинаковым режимом друг другу не мешают, и называть это
поломкой значило бы приучить пропускать весь раздел.

Рядом — второй раздел: правила, ссылающиеся на списки, которых нет.
Раньше загрузчик такие беды возвращал, а вызывающий их не читал.

## «Даже через VPN» при выключенном VPN

Шапка отчёта писала «движки остановлены», а раздел ниже — «закрыт полностью
даже через VPN». Туннеля в этом замере не было вовсе. Теперь так и сказано,
вместе с тем, что делать: запустить обход и повторить.

## itch.zone вернулся в проверку

Я принял его за зеркало по созвучию имени и выбросил. Это раздача файлов
и картинок самого itch.io, запись A у неё есть, и без неё отчёт про itch.io
был наполовину слеп.
