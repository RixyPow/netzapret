## Пустое окно при автозапуске

Задача автозапуска оставляла висеть пустое окно консоли на весь сеанс. Пустое
потому, что вывод уходит в журнал, а окно — потому что программа консольная,
и планировщик создаёт его сам. Настройка задачи `Hidden` тут не помогает: она
прячет задачу в списке планировщика, а не окно.

Теперь при запуске с журналом консоль отпускается сразу: смотреть в неё всё
равно некому. При обычном запуске из терминала она остаётся, как и ожидается.

## Выбор DNS

Новый пункт меню. Резолверы проверяются настоящим запросом DoH, а не пингом —
перекрывают именно запрос, а ICMP до 8.8.8.8 при этом ходит прекрасно.

```
  Cloudflare     1.1.1.1              141 мс   быстрый, без фильтрации
  Quad9          9.9.9.9              141 мс   отсекает вредоносные домены
  AdGuard        94.140.14.14         151 мс   отсекает рекламу и слежку
  Google         8.8.8.8              208 мс   надёжный <- сейчас
```

Список не сортируется по отклику и «быстрейший» не выбирается сам: резолвер
решает, какие ответы вы получите — один отсекает рекламу, другой вредоносные
домены, третий не фильтрует ничего. Это выбор, а не оптимизация.

Там же переключатель «разрешать имена через туннель» — на случай, когда
оператор перекроет DoH.

## Что ещё изменилось

**Команды перестали расходиться в том, что читают.** `where`, `config` и меню
брали оба слоя правил — базовый и ваш, — а `rules`, `test`, `watch` и `doctor`
только базовый. Хуже всего с `watch`: средство диагностики показывало решения,
отличные от настоящих.

**Испорченный файл правил больше не пропадает.** При неразобранном YAML загрузка
возвращала пустой набор, а следующее сохранение перезаписывало файл — то есть
опечатка стоила всех правок. Теперь файл отодвигается в `rules.user.yaml.broken`
до того, как что-либо успеет его затереть.

**Опечатка в ключе командной строки больше не проходит молча.** Раньше
неизвестная опция отбрасывалась, и команда делала не то, о чём её просили,
ничем этого не выдавая.

Из 0.2.0: маршруты по сервисам с разделением на части, работающие сайты
из файла hosts, MTU туннеля, целый журнал при двух экземплярах.

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются — их в архиве нет.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Диспетчер трафика для Windows: для каждого приложения, домена или подсети
решает, куда пустить трафик — через десинк, через VPN или напрямую.

**Применяет VPN точечно, не ломая десинк для всего остального.** Это и было
задачей: под поднятым туннелем рецепты Zapret с поддельными пакетами перестают
работать, потому что трафик выходит переоткрытым сокетом. Здесь в туннель
заводится только то, что адресовано проксируемым доменам, — остальное десинк
видит нетронутым.

## Установка

Скачать архив, распаковать, запустить `netzapret.exe`. Больше ничего ставить
не нужно: рантайм .NET, sing-box и Zapret входят в состав. Права администратора
программа запросит сама — без них не поднимутся ни туннель, ни драйвер
перехвата пакетов.

Понадобится только ссылка на подписку VPN — любого провайдера, поддерживаются
VLESS, Trojan, Hysteria2 и Shadowsocks.

## Что умеет

* пять режимов — от «только десинк» до «всё через VPN без исключений»;
* правила по программе, домену, адресу и спискам адресов, в двух слоях:
  выверенный базовый набор и ваш выбор поверх него;
* российские сервисы остаются напрямую даже в режиме «всё через VPN» —
  через зарубежный адрес они не работают;
* проверка серверов подписки с сортировкой по отклику;
* оба движка под одним присмотром, с перезапуском упавшего;
* автозапуск при входе в систему.

## Антивирус

Архив даёт 3 срабатывания из 66 на VirusTotal, и все три — на **WinDivert**,
драйвер перехвата пакетов, без которого обход DPI невозможен. Помечается как
`RiskTool` и `PUA`; Kaspersky пишет прямо — `Not-a-virus`. Та же картина
у Zapret и GoodbyeDPI.

Программа не подписана сертификатом издателя, поэтому Windows покажет
SmartScreen.

## Известные ограничения

* серверы VLESS + REALITY у некоторых провайдеров не подключаются —
  это ограничение sing-box, поддержка Xray вторым движком запланирована;
* установщика нет, только архив;
* интерфейс пока текстовый.

## Поддержать

Если пригодилось — [boosty.to/rixypow](https://boosty.to/rixypow/donate).
Программа бесплатная и останется такой.

## Лицензии

Сам NetZapret — MIT. Движки в составе остаются на своих условиях:
sing-box — GPL v3, Zapret — MIT, WinDivert и Cygwin — LGPL, `wintun.dll` —
проприетарная лицензия WireGuard LLC. Разбор — в `THIRD-PARTY.md` внутри архива.
