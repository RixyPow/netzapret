# Что нового

## Новое:

**Режимы работают строго по книге маршрутов.** Куда пойдёт каждый маршрут, теперь зависит от того, что включено:

| Включено | «напрямую» | «десинк» | «через VPN» |
|---|---|---|---|
| только десинк | напрямую | десинк | напрямую |
| десинк и туннель | напрямую | десинк | VPN |
| только туннель | напрямую | VPN | VPN |
| «игнорировать исключения» | VPN | VPN | VPN |

**«Игнорировать исключения» — всё через туннель.** Настройка «Отечественные сайты — тоже через туннель» отменяла только список российских сетей. Теперь она называется «Игнорировать исключения» и отправляет в туннель всё, включая «напрямую» и «десинк». Снаружи остаётся только домашняя сеть: роутер, принтер, сетевой диск. Десинку в этом режиме чинить нечего, и он не поднимается, даже если включён. Окно говорит об этом прямо на главной. Ваш прежний выбор сохраняется при обновлении.

## Исправления:

**Подписка больше не показывает ноль серверов, а программа не просит `rules.yaml`.** Программа запускается с правами администратора, и тогда Windows часто делает её рабочей папкой `C:\Windows\System32`. В этой папке тоже есть папка `config`, и программа принимала её за свою. Правила не находились, конфиг не собирался, подписка оставалась пустой. Теперь программа узнаёт свою папку по файлу `config\rules.yaml`. Ошибка была и в 0.6.4.

**«Напрямую» работает и при выключенном туннеле.** С одним десинком `winws2` получал вчерашний список исключений или не получал никакого. Поставленный «напрямую» сайт (например, Twitch) всё равно проходил через десинк, и перезапуск этого не менял. Теперь список исключений собирается при каждом запуске десинка.

**Список на вкладке «Маршруты» больше не зажат.** Карточка книги маршрутов отнимала у списка сервисов почти всю высоту. Теперь «Сохранить» и «Загрузить» стоят в одной строке с «Порядком вычисления», и список снова занимает окно.

## Чем это собрано и как сверить

Хеши заполняются при самом выпуске.

```text
NetZapret.zip   <SHA256>
NetZapret.exe   <SHA256>
```

Проверить скачанное:

```powershell
Get-FileHash NetZapret.zip -Algorithm SHA256
```

Собрать самому и сравнить:

```text
git checkout v0.7.1
pack.cmd
```

Совпасть должен `NetZapret.exe`. Архив — нет: в нём лежат winws2 и sing-box, собранные не нами, и их версии у вас могут отличаться.
