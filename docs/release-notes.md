## Выбранный режим наконец доходит до правил

Главное в выпуске, и найдено это разбором отчёта, а не кода.

Режим из меню **никогда не попадал в набор правил**. Загрузчик его
не принимал, поэтому все, кто им пользуется — включая сборку рабочего
конфига, — читали режим из `rules.yaml`, где написано `selective`.
Переключение «всё через VPN» меняло строку в меню и больше ничего.
Собственный комментарий загрузчика при этом утверждал, что «общие
настройки живут в меню»; передать их туда было нечем.

Вместе с режимом пересчитывается и умолчание — иначе «всё через VPN»
оставляло бы десинк всему, что не покрыто правилами, то есть большинству.

## Два режима VPN вели себя друг за друга

`ProxyAll`, названный «кроме РФ», правила не вычислял вовсе и отправлял
в туннель **и российские сервисы** — ради исключения которых он и назван.
А `ProxyStrict`, чей смысл именно в отсутствии исключений, правила читал.

Теперь они делают то, что написано:

| режим | правила «напрямую» | правила «десинк» | остальное |
| --- | --- | --- | --- |
| всё через VPN, кроме РФ | чтит | → туннель | туннель |
| всё через VPN без исключений | не чтит | не чтит | туннель |

Десинк в «кроме РФ» уступает туннелю намеренно: правило, написанное ради
обхода, там означает лишь «сервис закрыт», а чем открывать — решает режим.

## Проверка больше не говорит о туннеле там, где его нет

В режиме «только десинк» sing-box не поднимается вовсе, а правила
на именах остаются прежними. Отчёт читал их и помечал имена как идущие
через туннель, после чего винил туннель, которого не запускали: Telegram
выглядел бедой трубы, хотя упирался в прибитый адрес, который рвёт DPI.

Метка теперь спрашивает и режим тоже.

## Смена режима перезапускает движки

Режим меняет их состав и применяется только перезапуском. Прежде
программа останавливалась и ждала, когда человек сам нажмёт «Запустить»:
он менял настройку, видел «остановлено» и решал, что что-то сломал.

## Перебор адресов идёт разом и прерывается по Esc

Опрос шёл по одному: четыре резолвера на имя, каждый до таймаута, затем
каждый найденный адрес, тоже до таймаута, и всё это на десяток имён.
Минуты ожидания проб, которые друг от друга не зависят. Теперь они идут
вместе, по шесть, и Esc останавливает перебор, как и всякий другой замер
в программе.

Молчание всех адресов — тоже ответ, и о нём теперь говорится прямо.
Сервис, закрытый по адресу, разрешает имя честно и отказывает по самому
адресу; пин там бессилен, потому что задаёт, куда идти, а не создаёт
дорогу. Это WhatsApp, и лечит его туннель.

## Одна проверка серверов, одна память

Проверок было две, а помнила программа одну. Прогон семнадцати серверов
сообщал, что работают четыре; заход в выбор сервера показывал прежние
цифры либо предлагал измерить заново. Тот же замер, разная память,
и четырнадцать секунд ожидания впустую при каждом заходе.

Полная проверка теперь пишет в тот же кэш, который читает выбор. Неудачи
тоже: «этот не работает» — знание того же рода, что и отклик, и куплено
тем же ожиданием.
