## Голос Discord: чинили не то звено

Симптом — бесконечное «Подключение…» в голосовом канале при полностью
работающем сайте. Замер при работающих движках, один адрес Cloudflare,
меняется только SNI:

```
probe.discord.media    6066 мс   таймаут
discord.media            56 мс   рукопожатие прошло
discord.com              50 мс   рукопожатие прошло
gateway.discord.gg       54 мс   рукопожатие прошло
```

Три контрольных имени в чужих зонах, тоже несуществующих, отвечают за 55 мс.
Молчат ровно поддомены `discord.media`. Фильтр стоит на суффиксе имени,
и апекс зоны из-под него выпадает — потому «сайт открывается, а звонок
не идёт».

Войдя в канал, клиент получает от основного шлюза адрес голосового сервера
и открывает к нему WSS на `xxx.discord.media:443`. Рукопожатие не проходит —
и он остаётся на «Подключение…» навсегда. До UDP дело не доходит никогда:
адрес для IP discovery выдаёт как раз этот шлюз.

В пресете у Discord три секции, и правили мы всё это время не ту:

```
discord.media (voice RTC)   шлюз — оставался на send + syndata
Голосовые звонки/чаты       следующая стадия
Discord UDP                 её же запасной путь
```

Теперь у шлюза тот же рецепт, что у `discord.com`. Не наугад: перебор
одиннадцати рецептов дал три рабочих, и `hostfakesplit_multi` среди них.

Заодно в UDP-секции снято окно `-n8`: камуфляж держался на первые восемь
пакетов потока, а дальше звонок шёл голым до конца. Это отдельная правка
и вступит она в силу, когда звонок доходит до звука.

## Выбранный рецепт мог пропасть молча

Одинаковые наборы схлопываются в один рецепт, и зовётся он по первой секции
группы — а порядок задаёт файл пресета. Переставь секции, и вчерашний выбор
перестаёт находиться.

Тихо, что хуже всего: ненайденный рецепт означает пустой набор шагов,
профиль без шагов не выпускается вовсе, и настройка просто не применяется.
В меню при этом честно написано «десинк: hostfakesplit_multi».

Теперь рецепт ищется и среди имён групп, и среди секций внутри них,
а не найденный — пишется в журнал строкой с именем рецепта, именем пресета
и числом затронутых имён. Молчание тут и было главной бедой: сама поломка
на одну строку, а искали её три захода.

## Свой профиль берёт порты у секции пресета

Было зашито `80,443`. У Discord секции объявлены на восьми портах, и пять
из них — запасные HTTPS у Cloudflare: `2053,2083,2087,2096,8443`. Клиент
переходит на них сам, когда сеть ведёт себя плохо, то есть ровно в тех
условиях, ради которых обход и нужен.

Свой профиль встаёт перед пресетовскими и забирает имя себе — значит
обязан покрывать то же, что покрыла бы секция. Стоя уже, он брал часть
трафика, а остальное проходило вообще без обработки.

## Проверка рецептов у голоса

Не запускалась вовсе. Для замера бралась первая запись списка, а в
`discord-media.txt` она одна — сам `discord.media`, единственное имя зоны,
которое из-под фильтра выпадает. Проверка честно отвечала «рецепт не нужен».

Настоящее имя в каталог не впишешь: голосовые серверы выдаются под звонок,
и `frankfurt1234.discord.media` не разрешается ни у кого. Поэтому имя
для пробы задаёт каталог, а сама проба больше не спрашивает DNS: адрес
берётся у ближайшего предка, который разрешается, а имя уходит в SNI.
Списки winws2 сопоставляются по зонам, так что профиль встаёт тот же, что
на настоящий голосовой сервер.

Из перебора убраны секции по UDP. Рецепт проверяется рукопожатием TLS,
а ему в UDP проверять нечего: приём получал «не помогает» независимо
от собственных достоинств и стоял в списке наравне с работающими. Отбор
идёт по названному транспорту — секция без фильтров ловит всё подряд,
TCP в том числе.

## Пресет «Universal V8 diag»

Временный, для замера. Тот же V8 плюс `diag_once` в пяти секциях Discord:
строка на соединение — протокол, порты, адрес назначения, имя из SNI,
класс L7. Пакеты не трогает.

Плюс секция-ловушка на голосовой UDP в самом конце. Стоит последней
намеренно: первый совпавший профиль забирает пакет, и встань она раньше —
отобрала бы голос у секции выше. Пусто в ней — адрес голосового сервера
есть в списке и его забрали раньше; есть строки — список устарел и трафик
идёт мимо всех правил.

Журнал отдельный, `diag-discord.log` рядом с `winws2.exe`: `--debug` пишет
решение по каждому пакету, и минута звонка вытеснила бы из общего журнала
как раз то, ради чего он снимался.
