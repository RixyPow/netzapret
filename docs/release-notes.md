## Проверка спрашивает туннель, а не гадает о нём

Вердикт «туннель не доставил» ставился по неудаче соединения с адресом
fakeip — а это не то же самое, что «сервер подписки не дотянулся»: между нами
и туннелем стоит winws2. Отчёт пять прогонов подряд советовал сменить сервер,
ни разу этот сервер не спросив.

Теперь спрашивает. sing-box поднимается с локальным входом, без TUN и без прав
администратора; соединение к нему идёт по петле, которую winws2 не трогает,
а наружу уходит соединение к серверу подписки, чьего имени нет ни в одном
доменном списке. Ни один конец этого пути десинк испортить не может.

Замер на живой машине в день написания: **`whatsapp.net` и `whatsapp.com`
отвечают 302 через туннель за полсекунды**, тогда как через TUN числятся
недоставленными. Сервер исправен, беда на нашей стороне трубы.

Раздел «маршрутом не лечится» теперь так и говорит, вместо того чтобы
советовать смену сервера, опровергнутую замером двумя разделами выше.

## Каждому адресу имени — свой срок

У `updates.discord.com` пять адресов; один из них, `162.159.136.232`, закрыт,
четыре отвечают. Замер 2026-09-06: Google, Cloudflare и Яндекс возвращают все
пять и различаются лишь порядком — значит резолвер ни при чём, и сменой DNS
это не лечится.

Мы звали `ConnectAsync(host, port)` с одним сроком на всю попытку. Он
перебирает адреса сам, но закрытый молчит и съедает бюджет целиком, так что до
остальных дело не доходило. Имя объявлялось закрытым, тогда как настоящий
клиент открывает его со второй попытки и не замечает. Две машины на разных
операторах сообщали об этом, а запись сидела в списке мнимых отказов под
правдоподобным и неверным объяснением про «свой канал обновлений».

Теперь каждый адрес получает свой срок, а последующие пробы идут на тот,
который ответил.

## Отчёт пишется в файл при каждом прогоне

`reports\blockcheck-<дата>-<время>.txt` рядом с программой. Ключ `--out`
задаёт другое имя.

Время в имени затем, что сравнить нынешний прогон с прошлым — половина
разбора: сайт ломается **между** двумя проверками, а не во время одной.
Хранится двадцать; папка, растущая без предела, однажды будет вычищена целиком
вместе с тем отчётом, ради которого всё затевалось.

## Про антивирус говорится до запуска, а не после часа догадок

При запуске движков называется средство защиты со своим сетевым фильтром
и три способа, которыми оно мешает: фильтр встаёт на тот же слой, что и наш;
проверка защищённых соединений переустанавливает TLS своим клиентом; WinDivert
помечается как `RiskTool` и может уехать в карантин. Все три приходят молча.

Отдельно — про файл hosts. У одного из проверяющих Kaspersky вернул его
к своему умолчанию, стерев все пины; заметить удалось только по записке,
которую он оставил в комментарии. Проверка теперь эту записку узнаёт
и говорит прямо, а закрепление перечитывает файл сразу после записи: сообщать
об успехе того, чего больше не существует, — худшее, что средство настройки
может сделать.

## Сборку можно повторить у себя

В выпущенном `netzapret.exe` лежала строка `C:\Users\<имя>\Projects\...` —
путь сборочной машины вместе с именем пользователя. Она уезжала каждому
скачавшему и делала файл невоспроизводимым: другая папка давала другой двоичный
файл из тех же исходников.

Убрана. Версия пакета SDK закреплена в `global.json` — он меняет IL не меньше,
чем исходники, — и к выпуску прилагаются SHA256 архива и программы вместе
с версией, которой они собраны.

Совпасть должен `netzapret.exe`. Архив — нет: в нём winws2 и sing-box, собранные
не нами.

## Universal V7 стал основным

Тот же набор, что V6, плюс три правки по данным проверки — среди них рецепт
для Steam против обрыва потока после классификации. Смена касается только новых
установок: настройки переживают обновление намеренно.

## Мелочи, которые видно в отчёте

* `itch.zone`, `rutor.info` и `valorant.com` из показа убраны — они работают,
  а отчёт третий прогон подряд утверждал обратное. Маршруты сохранены.
* Пропущенное имя расходует свой слот. Прежде пропуск втягивал в проверку имя
  глубже по списку, часто такое же бесполезное: у GitHub вместо
  `githubusercontent.com` проверялся `githubassets.com`, и оба давали
  «нет адреса у имени».
* Вторым у Twitch стоит `live-video.net` — единственная из пяти зон раздачи,
  у которой запись A есть.

## Для тех, кто хочет сообщить о поломке

Появились `SECURITY.md` и формы issue. Форма спрашивает то, без чего разбор
не начинается: вывод проверки, режим, пресет, оператора и город. И отдельным
полем — «а что на самом деле», для случая, когда сервис работает,
а отчёт утверждает обратное. Такие сообщения ценнее всех прочих.
