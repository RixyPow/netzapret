## Проверка больше не винит DPI в том, что делает туннель

Из девяти вердиктов «DPI по TLS» в живом отчёте шесть стояли на именах,
заведённых в туннель. DPI их выдать не мог: он видит шифрованный поток
к серверу подписки и не знает, какое имя внутри. Измерялось на деле то,
что туннель не довёз — а совет к каждой строке прилагался про десинк,
то есть про вмешательство в рукопожатие, которого DPI и не видел.

Легенда это говорила прямо: «её строка описывает поведение туннеля,
а не сети». Вердикт об этом не знал.

Теперь для таких целей отдельный вид — **«туннель не доставил»**,
и лечение другое: сменить сервер подписки, а не рецепт. Отказ самого
сайта переименованию не поддался: 403 через туннель по-прежнему значит,
что отказал сайт, и терять этот вердикт нельзя — он единственный честно
указывает на страну.

## Зеркала убраны из отчёта

Списки Zapret держат домен рядом с зеркалами — это правильно для правила,
оба надо закрыть, — но не для отчёта. Человек читает «twitter.com закрыт»
и идёт чинить то, чем не пользуется, тогда как сам сервис открывается
под другим именем и двумя строками ниже помечен доступным. Из десяти
строк «маршрутом не лечится» шесть были про такие домены.

Не проверяются: `twitter.com`, `rutracker.net`, `notion.site`,
`riotgames.es`, `itch.zone`, `rutor.info`, `rutor.is`. Правила на них
продолжают действовать.

## Сервисы, чей отказ бывает мнимым

WhatsApp и Discord отвечают по своим адресам и портам, и отказ на 443
у них мало что значит. Оба числились сломанными три прогона подряд, работая
исправно. Прятать такие строки нельзя — отказ бывает и настоящим, — поэтому
они названы отдельно:

```
  Здесь отказ может быть мнимым — проверьте сами, прежде чем чинить:
    whatsapp.com               приложение ходит по своим адресам и порту 5222
    updates.discord.com        обновления идут своим каналом, не через 443
```

## Десинк с пином не спорит

Раздел «пин отменяет маршрут» помечал ошибкой одиннадцать имён, и девять
из них были в порядке. Десинк не меняет адрес назначения — он вмешивается
в пакеты по дороге к нему, а сам адрес по-прежнему берётся из hosts.
Отменяет пин только «через VPN», где прибитый адрес уводится в туннель.

Поймано наблюдением, а не рассуждением: у прибитого `tr.rbxcdn.com`
правило «десинк», раздел звал это ошибкой настройки — а превью грузятся.

## Меню

`DNS` и `Файл hosts` сведены в один пункт **«Имена и адреса»**: оба про
одно — каким адресом окажется имя, — а стояли через строку, и пин
ставился в одном разделе, а смотрелся в другом. Главное меню стало
короче на строку, пункты после четвёртого сдвинулись.

Из редактора hosts убран пункт, гасивший записи «в пользу каталога»:
он существовал ради пути через резолвер, а тот снят в прошлом выпуске.

## Каталог

Превью Roblox — двумя записями: адресом, который проверен на живой машине,
и вариантом «спросить честный резолвер» рядом, на случай если тот адрес
перестанет отвечать.

## Мелкое

* Полученное тело считается законченным ответом: сервер, отдавший всё
  обещанное и не закрывший соединение, объявлялся оборванным потоком.
