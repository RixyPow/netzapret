## Universal V8 стал пресетом по умолчанию

Отличается от V7 одним — рецептом Discord, — но именно этот рецепт на текущих
сетях и решает, открывается сервис или нет. Перебор одиннадцати вариантов дал
три рабочих, и `hostfakesplit_multi` среди них; прежний `send + syndata`
не открывал ни `discord.com`, ни `updates.discord.com`.

Смена касается только новых установок. Настройки переживают обновление
намеренно — в них работа человека, — поэтому у тех, кто уже запускался,
остаётся прежний выбор. Поменять его можно в «Десинке», V8 стоит в списке
рядом с V7.

## `updates.discord.com` молчал, и от этого висел клиент

Замер после того, как со списка Discord сняли свой профиль. Отвечало всё:
`discord.com` — 200, `gateway.discord.gg` — 404, `cdn.discordapp.com` — 403,
`media.discordapp.net` — 401, `discordapp.com` и `discord.gg` — 301. Коды
разные, но это ответы сервера, то есть рукопожатие проходит. Молчал ровно один
адрес: `updates.discord.com`, таймаут восемь секунд.

Туда стучится десктопный клиент при запуске, и пока ответа нет, он висит
на проверке обновлений. Снаружи это и выглядит как «Discord не работает»
при полностью открывающемся сайте.

Причина — порядок. У `updates.discord.com` в пресете своя секция, и она стоит
раньше правленого `discord.com`; побеждает первый совпавший профиль, до второго
дело не доходит никогда. Свой профиль это чинил потому, что стоял раньше всех
и нёс зону `discord.com`, которая покрывает и `updates`. Теперь рецепт
исправлен в обеих секциях.

`discord.media` и правило по адресам оставлены как были: первый отвечает 520,
то есть рукопожатие у него проходит и ломаться нечему, а второй про голос
и лечится иначе.

## Discord в каталоге — одна часть вместо трёх

«Картинки и вложения» и «Обновления» решали судьбу имён, которые и так решены
соседней строкой. Записи в списках — зоны: `discordapp.net` из `discord.txt`
покрывает и `media.discordapp.net`, и обе `images-ext`, а `discord.com`
покрывает `updates.discord.com`. Какая из двух строк победит, зависело
от их порядка в файле — то есть от того, чего в меню не видно.

Увидеть это удалось ровно в тот момент, когда с «текста и входа» сняли рецепт:
отвалился весь Discord разом, включая картинки и обновления, у которых свой
маршрут стоял нетронутым.

Теперь одна часть — «Сайт и переписка», всё кроме голоса. Голос остался
отдельно, и он правда ломается иначе: ходит по адресам, имени в тех пакетах
нет вовсе.

## Тема меняется сразу

Менялась она через раз, и причина в WPF: `StaticResource` разрешается один раз
при загрузке, поэтому подмена палитры не действовала на уже нарисованное.
Открытые разделы держали прежние цвета до тех пор, пока их не пересоздадут.
Все ссылки на палитру переведены на `DynamicResource` — 136 мест в разметке
и 62 в стилях. Сами стили остались статическими: меняются не они, а кисти
внутри них.

Выбранная тема отмечена обводкой, а не только цветом подписи. У двух кнопок
рядом разница в оттенке текста читается как «одна поярче», а не как выбор.
