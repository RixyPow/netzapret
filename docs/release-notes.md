# Что нового

## Исправления:

**Подписки, которые работают «только в Happ», теперь читаются.** Некоторые провайдеры отдают подписку только клиенту, который называет себя Happ и передаёт номер устройства, — всем остальным отвечают «404 Not Found». NetZapret получал этот отказ и писал «Не удалось загрузить». Теперь при таком отказе программа повторяет запрос как Happ с тем же номером устройства — и запоминает это: следующее чтение той же подписки сразу идёт под нужным именем. Если провайдер снова начнёт отвечать всем, память сама забудется.

Сама ссылка при этом нигде не сохраняется второй раз: память хранит только её отпечаток.
