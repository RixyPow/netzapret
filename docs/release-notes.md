# Что нового

## Новое:

**Game filter.** Тумблер в разделе «Десинк»: к любому пресету добавляются секции для игр — как у Flowseal. Трогаются только первые пакеты соединений к облакам и хостингам, дальше игра идёт нетронутой. Экспериментальный и выключен по умолчанию: выключенный не меняет в работе движка ничего.

**Состояние движков — честнее и нагляднее.** На «Главной» состояние каждого движка теперь прямо в его карточке, «Десинк» или «Туннель», с номером процесса. Если выходы VPN не отвечают, так и написано — «выходы подписки не отвечают, трафик идёт мимо туннеля · с 23:30», — а не «работает». То же в меню трея.

**Отчёт для разбора — с проверкой блокировок.** В архив теперь входит последняя проверка блокировок, а карточка отчёта стоит первой в «Ещё». Имена файлов внутри — латиницей, чтобы открывались в любом распаковщике.

## Исправления:

**Туннель поднимается, если роутер раздаёт DNS по IPv6.** У таких пользователей sing-box отвергал конфиг целиком, и VPN не работал вовсе.

**Меньше пустых попыток по IPv6.** Там, где IPv6 нет, движок сначала пробует IPv4 и не тратит время на заведомо недоступные адреса.

**Журнал говорит, кто поднял или остановил движки** — кнопка, трей, автозапуск или обновление. Это помогает разбирать неисправности.
