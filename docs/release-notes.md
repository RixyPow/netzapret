## Проверка стала быстрее и прерываемой

Пробы идут разом, а не по очереди. Проверка почти целиком состоит из ожидания:
живой хост отвечает за доли секунды, мёртвый молчит все четыре, и сорок целей
подряд — это минуты, проведённые ни за чем. Полный прогон занимал около четырёх
минут, теперь — **51 секунду**, при вдвое большем числе целей.

**Esc** прерывает и показывает то, что успело набраться. Раньше оставалось
только закрыть окно, потеряв всё.

Три режима:

```
  1. Быстрая — по имени с каждой части, около полуминуты
  2. Полная — по два имени, дольше, но подробнее
  3. Точечная — один сервис, зато целиком
```

Из командной строки — `--quick`, `--full`, `--service <имя>`.

## Прибитые имена в hosts

ChatGPT перестал открываться, и ни десинк, ни VPN не помогали. Причина
оказалась не в блокировке: в файле hosts прибито **670 имён к одному адресу**,
и адрес этот давно не отвечает. Среди них весь OpenAI.

Запись в hosts сильнее любой нашей настройки: имя разрешается ещё до нашего
резолвера, fakeip не выдаётся, и трафик уходит по прибитому адресу мимо
туннеля. Со стороны неотличимо от блокировки.

Проверка теперь показывает такие имена отдельным разделом и не даёт по ним
советов: пока запись на месте, маршрут ничего не решает.

Здесь же нашлась и наша ошибка. Мы умеем заводить прибитый адрес в туннель,
чтобы правило всё же сработало, — но делали это только для доменных правил.
Раздел сервисов пишет списочные, и для всего, что назначено через него, это
молча не работало. Теперь учитываются оба вида.

## Что ещё изменилось

**Подсказка про число правил согласована с числом** — было «Записать эти
1 правило».

## Обновление

Распакуйте новый архив поверх старого. Настройки и правила в `config\`
не перезаписываются — их в архиве нет.

После обновления выключите и включите автозапуск, чтобы задача перепрописалась.

---

Что это такое, как поставить с нуля, почему ругается антивирус и на каких
условиях распространяется — в [README](https://github.com/RixyPow/netzapret)
и в файлах внутри архива.
